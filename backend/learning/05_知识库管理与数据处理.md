# 第5章：知识库管理与数据处理

> 🎯 **学习目标**：深入理解知识库系统架构、文档上传处理流程、数据分块策略、向量化处理以及权限管理
> 💡 **学习重点**：完整的知识库管理工作流、数据处理最佳实践

---

## 5.1 知识库系统架构

### 5.1.1 整体架构

```
┌─────────────────────────────────────────────────────────────────────┐
│                    知识库系统整体架构                                │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐  │
│  │  前端层（Enterprise Portal）                                 │  │
│  │  - 知识库管理 UI                                             │  │
│  │  - 文档上传 UI                                               │  │
│  │  - 检索测试 UI                                               │  │
│  └──────────────────────┬──────────────────────────────────────┘  │
│                         │                                         │
│                         ▼                                         │
│  ┌─────────────────────────────────────────────────────────────┐  │
│  │  API Gateway                                                │  │
│  │  - 认证授权                                                 │  │
│  │  - 限流熔断                                                 │  │
│  │  - 请求路由                                                 │  │
│  └──────────────────────┬──────────────────────────────────────┘  │
│                         │                                         │
│                         ▼                                         │
│  ┌─────────────────────────────────────────────────────────────┐  │
│  │  Knowledge Service（核心服务）                               │  │
│  │  ┌───────────────────────────────────────────────────────┐  │  │
│  │  │  知识库管理模块                                        │  │  │
│  │  │  - 创建/编辑/删除知识库                                │  │  │
│  │  │  - 知识库配置管理                                      │  │  │
│  │  └───────────────────────┬───────────────────────────────┘  │  │
│  │                          │                                    │  │
│  │  ┌───────────────────────▼───────────────────────────────┐  │  │
│  │  │  文档处理模块                                         │  │  │
│  │  │  - 文档上传与存储                                      │  │  │
│  │  │  - 文本提取（PDF/TXT/Word）                            │  │  │
│  │  │  - 数据清洗与预处理                                    │  │  │
│  │  └───────────────────────┬───────────────────────────────┘  │  │
│  │                          │                                    │  │
│  │  ┌───────────────────────▼───────────────────────────────┐  │  │
│  │  │  分块与向量化模块                                     │  │  │
│  │  │  - 智能分块（按句子/段落）                             │  │  │
│  │  │  - 向量生成（Embedding Model）                        │  │  │
│  │  │  - 向量存储（ChromaDB）                                │  │  │
│  │  └───────────────────────┬───────────────────────────────┘  │  │
│  │                          │                                    │  │
│  │  ┌───────────────────────▼───────────────────────────────┐  │  │
│  │  │  检索模块                                             │  │  │
│  │  │  - 向量检索                                           │  │  │
│  │  │  - 混合检索                                           │  │  │
│  │  │  - 重排序                                             │  │  │
│  │  └───────────────────────────────────────────────────────┘  │  │
│  └──────────────────────┬──────────────────────────────────────┘  │
│                         │                                         │
│         ┌───────────────┼───────────────┐                         │
│         ▼               ▼               ▼                         │
│  ┌──────────┐    ┌──────────┐    ┌──────────┐                  │
│  │PostgreSQL│    │ Redis    │    │ChromaDB  │                  │
│  │(元数据)  │    │(缓存)    │    │(向量)    │                  │
│  └──────────┘    └──────────┘    └──────────┘                  │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 5.1.2 数据模型关系

```
┌─────────────────────────────────────────────────────────────────────┐
│                    数据模型关系图                                    │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────────────┐                                               │
│  │   Enterprise     │                                               │
│  │   (企业)         │                                               │
│  │  - id (PK)       │                                               │
│  │  - name          │                                               │
│  └────────┬─────────┘                                               │
│           │                                                         │
│           │ 1:N                                                     │
│           ▼                                                         │
│  ┌──────────────────┐                                               │
│  │  KnowledgeBase   │                                               │
│  │   (知识库)       │                                               │
│  │  - id (PK)       │                                               │
│  │  - enterprise_id │                                               │
│  │  - name          │                                               │
│  │  - is_active     │                                               │
│  └────────┬─────────┘                                               │
│           │                                                         │
│           │ 1:N                                                     │
│           ▼                                                         │
│  ┌──────────────────┐                                               │
│  │    Document      │                                               │
│  │    (文档)        │                                               │
│  │  - id (PK)       │                                               │
│  │  - knowledge_base│                                               │
│  │  - filename      │                                               │
│  │  - status        │                                               │
│  │  - file_hash     │                                               │
│  └────────┬─────────┘                                               │
│           │                                                         │
│           │ 1:N                                                     │
│           ▼                                                         │
│  ┌──────────────────┐                                               │
│  │      Chunk       │  (逻辑上存在，存储在ChromaDB)                 │
│  │    (文档片段)    │                                               │
│  │  - id            │                                               │
│  │  - document_id   │                                               │
│  │  - content       │                                               │
│  │  - embedding     │                                               │
│  └──────────────────┘                                               │
│                                                                     │
│  ┌──────────────────┐                                               │
│  │       FAQ        │                                               │
│  │    (问答对)      │                                               │
│  │  - id (PK)       │                                               │
│  │  - enterprise_id │                                               │
│  │  - question      │                                               │
│  │  - answer        │                                               │
│  └──────────────────┘                                               │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 5.2 文档上传与处理流程

### 5.2.1 完整处理流水线

```
┌─────────────────────────────────────────────────────────────────────┐
│                    文档处理完整流程                                  │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  用户上传文档                                                       │
│         │                                                           │
│         ▼                                                           │
│  ┌──────────────────────────────────────────────────────────┐     │
│  │  1. 文件验证                                             │     │
│  │  - 文件大小检查（<50MB）                                 │     │
│  │  - 文件类型检查（PDF/TXT/DOCX）                          │     │
│  │  - 文件哈希计算（去重）                                  │     │
│  └─────────────────────┬──────────────────────────────────┘     │
│                        │                                          │
│                        ▼                                          │
│  ┌──────────────────────────────────────────────────────────┐     │
│  │  2. 文本提取                                             │     │
│  │  - PDF → 使用 PyPDF2                                    │     │
│  │  - DOCX → 使用 python-docx                              │     │
│  │  - TXT → 直接读取                                        │     │
│  └─────────────────────┬──────────────────────────────────┘     │
│                        │                                          │
│                        ▼                                          │
│  ┌──────────────────────────────────────────────────────────┐     │
│  │  3. 数据清洗                                             │     │
│  │  - 去除多余空白字符                                      │     │
│  │  - 去除特殊符号                                          │     │
│  │  - 标准化格式                                            │     │
│  └─────────────────────┬──────────────────────────────────┘     │
│                        │                                          │
│                        ▼                                          │
│  ┌──────────────────────────────────────────────────────────┐     │
│  │  4. 智能分块                                             │     │
│  │  - 按段落/句子分割                                       │     │
│  │  - 每个 chunk 500-1000 字符                              │     │
│  │  - 重叠 50-100 字符                                      │     │
│  └─────────────────────┬──────────────────────────────────┘     │
│                        │                                          │
│                        ▼                                          │
│  ┌──────────────────────────────────────────────────────────┐     │
│  │  5. 向量化                                               │     │
│  │  - 批量调用 Embedding Model                             │     │
│  │  - 生成 768 维向量                                      │     │
│  └─────────────────────┬──────────────────────────────────┘     │
│                        │                                          │
│                        ▼                                          │
│  ┌──────────────────────────────────────────────────────────┐     │
│  │  6. 存储                                                 │     │
│  │  - 元数据 → PostgreSQL                                   │     │
│  │  - 向量 → ChromaDB                                       │     │
│  │  - 更新知识库统计                                        │     │
│  └─────────────────────┬──────────────────────────────────┘     │
│                        │                                          │
│                        ▼                                          │
│  ┌──────────────────────────────────────────────────────────┐     │
│  │  7. 状态更新                                             │     │
│  │  - 成功 → completed                                     │     │
│  │  - 失败 → failed + 错误信息                             │     │
│  └──────────────────────────────────────────────────────────┘     │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 5.2.2 分块策略详解

```
┌─────────────────────────────────────────────────────────────────────┐
│                    分块策略对比                                      │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  策略1：固定长度分块（Simple）                                       │
│  ┌─────────────────────────────────────────────────────────┐      │
│  │                                                          │      │
│  │  方法：每 N 个字符切一刀                                 │      │
│  │                                                          │      │
│  │  优点：                                                 │      │
│  │  ✅ 实现简单                                            │      │
│  │  ✅ 计算快                                              │      │
│  │                                                          │      │
│  │  缺点：                                                 │      │
│  │  ❌ 可能在句子中间截断，语义不完整                       │      │
│  │  ❌ 可能破坏段落结构                                    │      │
│  │                                                          │      │
│  └─────────────────────────────────────────────────────────┘      │
│                                                                     │
│  策略2：句子级别分块（Better）                                       │
│  ┌─────────────────────────────────────────────────────────┐      │
│  │                                                          │      │
│  │  方法：                                                 │      │
│  │  1. 按句子分割文本                                      │      │
│  │  2. 合并句子直到达到 chunk_size                        │      │
│  │                                                          │      │
│  │  优点：                                                 │      │
│  │  ✅ 保持句子完整性                                      │      │
│  │  ✅ 语义更完整                                          │      │
│  │                                                          │      │
│  │  缺点：                                                 │      │
│  │  ❌ 块大小不均匀                                        │      │
│  │                                                          │      │
│  └─────────────────────────────────────────────────────────┘      │
│                                                                     │
│  策略3：段落级别分块（Best）                                         │
│  ┌─────────────────────────────────────────────────────────┐      │
│  │                                                          │      │
│  │  方法：                                                 │      │
│  │  1. 按段落（换行符）分割                                │      │
│  │  2. 合并段落直到达到 chunk_size                        │      │
│  │  3. 添加重叠（overlap）                                │      │
│  │                                                          │      │
│  │  优点：                                                 │      │
│  │  ✅ 保持段落结构完整性                                  │      │
│  │  ✅ 语义最完整                                          │      │
│  │  ✅ 检索效果最好                                        │      │
│  │                                                          │      │
│  └─────────────────────────────────────────────────────────┘      │
│                                                                     │
│  本项目使用：策略3（段落级别分块）                                   │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 5.2.3 分块代码实现

**文件位置**：`backend/knowledge-service/chunker.py`

```python
from typing import List, Dict, Any
import re
from common.config import settings


class TextChunker:
    """Text chunking for RAG"""
    
    def __init__(
        self,
        chunk_size: int = None,
        chunk_overlap: int = None,
        separator: str = "\n"
    ):
        self.chunk_size = chunk_size or settings.CHUNK_SIZE
        self.chunk_overlap = chunk_overlap or settings.CHUNK_OVERLAP
        self.separator = separator
    
    def chunk_text(self, text: str, metadata: Dict[str, Any] = None) -> List[Dict[str, Any]]:
        """
        Split text into chunks using paragraph-level chunking
        
        Args:
            text: Raw text to chunk
            metadata: Metadata to attach to each chunk
            
        Returns:
            List of chunks with content and metadata
        """
        chunks = []
        
        # Step 1: Split by separator (paragraphs)
        paragraphs = re.split(self.separator, text)
        # Filter out empty paragraphs
        paragraphs = [p.strip() for p in paragraphs if p.strip()]
        
        current_chunk = ""
        chunk_id = 0
        
        for paragraph in paragraphs:
            # Check if adding this paragraph exceeds chunk size
            if len(current_chunk) + len(paragraph) + 1 <= self.chunk_size:
                # Can add to current chunk
                current_chunk += paragraph + " "
            else:
                # Save current chunk if not empty
                if current_chunk.strip():
                    chunks.append({
                        "content": current_chunk.strip(),
                        "chunk_id": str(chunk_id),
                        "metadata": metadata or {}
                    })
                    chunk_id += 1
                
                # Start new chunk with overlap
                overlap_text = ""
                if len(current_chunk) > self.chunk_overlap:
                    overlap_text = current_chunk[-self.chunk_overlap:]
                else:
                    overlap_text = current_chunk
                
                current_chunk = overlap_text + paragraph + " "
        
        # Don't forget the last chunk
        if current_chunk.strip():
            chunks.append({
                "content": current_chunk.strip(),
                "chunk_id": str(chunk_id),
                "metadata": metadata or {}
            })
        
        return chunks
    
    def chunk_documents(self, documents: List[Dict[str, Any]]) -> List[Dict[str, Any]]:
        """
        Chunk multiple documents
        
        Args:
            documents: List of documents with content and metadata
            
        Returns:
            Flattened list of chunks from all documents
        """
        all_chunks = []
        
        for doc in documents:
            metadata = {
                "document_id": doc.get("id"),
                "filename": doc.get("filename"),
                **doc.get("metadata", {})
            }
            chunks = self.chunk_text(doc["content"], metadata)
            all_chunks.extend(chunks)
        
        return all_chunks
```

### 5.2.4 文本提取函数

```python
def extract_text_from_file(file_path: str) -> str:
    """
    Extract text from various file formats
    
    Supports: PDF, TXT, DOCX
    
    Args:
        file_path: Path to the file
        
    Returns:
        Extracted text content
        
    Raises:
        ValueError: If file format is not supported
    """
    try:
        # Extract based on file extension
        if file_path.lower().endswith('.pdf'):
            return extract_text_from_pdf(file_path)
        elif file_path.lower().endswith('.txt'):
            return extract_text_from_txt(file_path)
        elif file_path.lower().endswith('.docx'):
            return extract_text_from_docx(file_path)
        else:
            raise ValueError(f"Unsupported file format: {file_path}")
    except ImportError as e:
        raise ValueError(f"Missing dependency for file extraction: {e}")


def extract_text_from_pdf(file_path: str) -> str:
    """Extract text from PDF file using PyPDF2"""
    try:
        import PyPDF2
        text = ""
        with open(file_path, 'rb') as f:
            reader = PyPDF2.PdfReader(f)
            for page_num, page in enumerate(reader.pages):
                page_text = page.extract_text()
                if page_text:
                    # Add page number to metadata context
                    text += f"\n[Page {page_num + 1}]\n"
                    text += page_text
        return text
    except Exception as e:
        raise ValueError(f"Failed to extract text from PDF: {e}")


def extract_text_from_txt(file_path: str) -> str:
    """Extract text from plain text file"""
    try:
        with open(file_path, 'r', encoding='utf-8') as f:
            return f.read()
    except UnicodeDecodeError:
        # Try with different encoding
        with open(file_path, 'r', encoding='gbk') as f:
            return f.read()


def extract_text_from_docx(file_path: str) -> str:
    """Extract text from Word document"""
    try:
        from docx import Document
        doc = Document(file_path)
        text = ""
        for para in doc.paragraphs:
            text += para.text + "\n"
        return text
    except Exception as e:
        raise ValueError(f"Failed to extract text from DOCX: {e}")
```

---

## 5.3 向量化处理

### 5.3.1 向量化流程

```
┌─────────────────────────────────────────────────────────────────────┐
│                    向量化处理流程                                    │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  Chunk 列表                                                         │
│      │                                                              │
│      ▼                                                              │
│  ┌─────────────────────────────────────────────────────────────┐  │
│  │  批量分桶（Batch Processing）                               │  │
│  │  - 每批 32 个 chunks                                       │  │
│  │  - 避免显存溢出                                           │  │
│  └─────────────────────┬───────────────────────────────────────┘  │
│                        │                                           │
│                        ▼                                           │
│  ┌─────────────────────────────────────────────────────────────┐  │
│  │  调用 Embedding Model                                      │  │
│  │  - 模型：shibing624/text2vec-base-chinese                  │  │
│  │  - 输出：768 维向量                                        │  │
│  │  - 归一化：是（便于余弦相似度计算）                         │  │
│  └─────────────────────┬───────────────────────────────────────┘  │
│                        │                                           │
│                        ▼                                           │
│  ┌─────────────────────────────────────────────────────────────┐  │
│  │  存储到 ChromaDB                                            │  │
│  │  - ID: {knowledge_base_id}_{chunk_id}                      │  │
│  │  - Document: chunk content                                │  │
│  │  - Embedding: 768 维向量                                   │  │
│  │  - Metadata: {filename, page, knowledge_base_id}          │  │
│  └─────────────────────┬───────────────────────────────────────┘  │
│                        │                                           │
│                        ▼                                           │
│  ┌─────────────────────────────────────────────────────────────┐  │
│  │  更新统计信息                                                │  │
│  │  - document.chunk_count += N                               │  │
│  │  - knowledge_base.chunk_count += N                         │  │
│  └─────────────────────────────────────────────────────────────┘  │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 5.3.2 向量化代码实现

**文件位置**：`backend/knowledge-service/embeddings.py`

```python
from typing import List, Optional
from sentence_transformers import SentenceTransformer
from common.config import settings


class EmbeddingModel:
    """
    Wrapper for embedding model with lazy loading
    
    Features:
    - Lazy loading (only load when needed)
    - Batch processing
    - Normalized embeddings
    """
    
    def __init__(self, model_name: str = None):
        self.model_name = model_name or settings.EMBEDDING_MODEL
        self._model = None
    
    @property
    def model(self):
        """
        Lazy load the embedding model
        
        Returns:
            SentenceTransformer model instance
        """
        if self._model is None:
            print(f"Loading embedding model: {self.model_name}")
            self._model = SentenceTransformer(self.model_name)
        return self._model
    
    def encode(
        self,
        texts: List[str],
        normalize: bool = True,
        batch_size: int = 32
    ) -> List[List[float]]:
        """
        Generate embeddings for a list of texts
        
        Args:
            texts: List of text strings to encode
            normalize: Whether to normalize embeddings to unit length
            batch_size: Batch size for processing
            
        Returns:
            List of embeddings, each is a list of floats
        """
        if not texts:
            return []
        
        embeddings = self.model.encode(
            texts,
            normalize_embeddings=normalize,
            batch_size=batch_size,
            show_progress_bar=False
        )
        
        return embeddings.tolist()
    
    def get_dimension(self) -> int:
        """
        Get the dimension of the embedding vectors
        
        Returns:
            Integer dimension (e.g., 768)
        """
        return self.model.get_sentence_embedding_dimension()


# Singleton instance
_embedding_model: Optional[EmbeddingModel] = None


def get_embedding_model() -> EmbeddingModel:
    """
    Get or create the singleton embedding model instance
    
    Returns:
        EmbeddingModel instance
    """
    global _embedding_model
    if _embedding_model is None:
        _embedding_model = EmbeddingModel()
    return _embedding_model


def generate_embeddings(texts: List[str]) -> List[List[float]]:
    """
    Convenience function to generate embeddings
    
    Args:
        texts: List of text strings
        
    Returns:
        List of embeddings
    """
    model = get_embedding_model()
    return model.encode(texts)
```

---

## 5.4 知识库权限管理

### 5.4.1 权限模型

```
┌─────────────────────────────────────────────────────────────────────┐
│                    知识库权限模型                                    │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  角色（Role）定义：                                                  │
│  ┌─────────────────────────────────────────────────────────┐      │
│  │                                                          │      │
│  │  1. SUPER_ADMIN（超级管理员）                            │      │
│  │     - 所有权限                                           │      │
│  │     - 跨企业管理                                         │      │
│  │                                                          │      │
│  │  2. ADMIN（企业管理员）                                  │      │
│  │     - 管理本企业所有知识库                               │      │
│  │     - 管理本企业用户                                     │      │
│  │                                                          │      │
│  │  3. EDITOR（编辑者）                                     │      │
│  │     - 创建/编辑/删除知识库                               │      │
│  │     - 上传/删除文档                                      │      │
│  │     - 不能管理用户                                       │      │
│  │                                                          │      │
│  │  4. VIEWER（查看者）                                     │      │
│  │     - 查看知识库                                         │      │
│  │     - 测试检索                                           │      │
│  │     - 不能修改任何内容                                   │      │
│  │                                                          │      │
│  └─────────────────────────────────────────────────────────┘      │
│                                                                     │
│  权限矩阵：                                                         │
│  ┌──────────────┬────────┬────────┬────────┬────────┐             │
│  │  操作        │SUPER_ADMIN│ADMIN  │EDITOR  │VIEWER  │             │
│  ├──────────────┼────────┼────────┼────────┼────────┤             │
│  │  创建知识库  │  ✓     │  ✓     │  ✓     │  ✗    │             │
│  │  编辑知识库  │  ✓     │  ✓     │  ✓     │  ✗    │             │
│  │  删除知识库  │  ✓     │  ✓     │  ✓     │  ✗    │             │
│  │  上传文档    │  ✓     │  ✓     │  ✓     │  ✗    │             │
│  │  删除文档    │  ✓     │  ✓     │  ✓     │  ✗    │             │
│  │  查看知识库  │  ✓     │  ✓     │  ✓     │  ✓    │             │
│  │  检索测试    │  ✓     │  ✓     │  ✓     │  ✓    │             │
│  │  管理用户    │  ✓     │  ✓     │  ✗     │  ✗    │             │
│  └──────────────┴────────┴────────┴────────┴────────┘             │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 5.4.2 权限检查实现

```python
from enum import Enum
from typing import Optional
from sqlalchemy.orm import Session
from common.rbac import check_permission, Role, Permission


class KnowledgeBasePermission(Enum):
    """Knowledge base specific permissions"""
    CREATE = "knowledge_base:create"
    READ = "knowledge_base:read"
    UPDATE = "knowledge_base:update"
    DELETE = "knowledge_base:delete"
    UPLOAD_DOCUMENT = "knowledge_base:upload_document"
    DELETE_DOCUMENT = "knowledge_base:delete_document"


def check_kb_permission(
    db: Session,
    user_id: str,
    enterprise_id: str,
    knowledge_base_id: Optional[str] = None,
    permission: KnowledgeBasePermission = KnowledgeBasePermission.READ
) -> bool:
    """
    Check if user has permission for knowledge base operation
    
    Args:
        db: Database session
        user_id: User ID
        enterprise_id: Enterprise ID
        knowledge_base_id: Knowledge base ID (optional)
        permission: Permission to check
        
    Returns:
        True if user has permission, False otherwise
    """
    # First check RBAC role permissions
    has_general_permission = check_permission(
        db=db,
        user_id=user_id,
        enterprise_id=enterprise_id,
        permission=permission.value
    )
    
    if not has_general_permission:
        return False
    
    # If no specific KB, general permission is enough
    if not knowledge_base_id:
        return True
    
    # TODO: Add KB-specific ownership checks if needed
    # For example: only the owner of a KB can delete it
    
    return True
```

---

## 5.5 数据去重与版本管理

### 5.5.1 文件去重策略

```
┌─────────────────────────────────────────────────────────────────────┐
│                    文件去重策略                                      │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  去重方法：                                                         │
│  ┌─────────────────────────────────────────────────────────┐      │
│  │                                                          │      │
│  │  使用文件哈希（SHA-256）                                │      │
│  │                                                          │      │
│  │  流程：                                                 │      │
│  │  1. 计算上传文件的 SHA-256 哈希                         │      │
│  │  2. 在数据库中查找相同 enterprise_id + file_hash 的记录 │      │
│  │  3. 如果存在 → 跳过上传，返回现有文档                   │      │
│  │  4. 如果不存在 → 继续处理                               │      │
│  │                                                          │      │
│  │  优点：                                                 │      │
│  │  ✅ 完全去重，节省存储空间                               │      │
│  │  ✅ 避免重复计算向量化                                   │      │
│  │  ✅ 快速判断（索引查询）                                 │      │
│  │                                                          │      │
│  └─────────────────────────────────────────────────────────┘      │
│                                                                     │
│  注意事项：                                                         │
│  - 文件名不同但内容相同 → 认为是同一个文件                          │
│  - 内容稍有不同 → 认为是不同文件                                    │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 5.5.2 版本管理

```python
from datetime import datetime
from sqlalchemy.orm import Session
from knowledge_service.models import Document, DocumentVersion


def create_document_version(db: Session, document: Document) -> DocumentVersion:
    """
    Create a new version of a document
    
    Args:
        db: Database session
        document: Current document instance
        
    Returns:
        Created DocumentVersion instance
    """
    version = DocumentVersion(
        document_id=document.id,
        version_number=document.version + 1,
        content=document.content,
        metadata=document.metadata_,
        created_at=datetime.utcnow()
    )
    
    db.add(version)
    db.commit()
    db.refresh(version)
    
    return version


def restore_document_version(db: Session, document_id: str, version_number: int) -> Optional[Document]:
    """
    Restore a document to a previous version
    
    Args:
        db: Database session
        document_id: Document ID
        version_number: Version number to restore
        
    Returns:
        Updated Document instance, or None if not found
    """
    # Find the version
    version = db.query(DocumentVersion).filter(
        DocumentVersion.document_id == document_id,
        DocumentVersion.version_number == version_number
    ).first()
    
    if not version:
        return None
    
    # Find the document
    document = db.query(Document).filter(Document.id == document_id).first()
    
    if not document:
        return None
    
    # Create backup of current version first
    create_document_version(db, document)
    
    # Restore
    document.content = version.content
    document.metadata_ = version.metadata
    document.updated_at = datetime.utcnow()
    
    db.commit()
    db.refresh(document)
    
    return document
```

---

## 5.6 最佳实践总结

### 5.6.1 文档处理最佳实践

| 方面 | 建议 | 说明 |
|------|------|------|
| 文件大小 | <50MB | 避免处理超大文件，影响性能 |
| Chunk 大小 | 500-1000 字符（中文） | 平衡上下文完整性和检索精度 |
| Overlap | 50-100 字符 | 避免语义截断 |
| 支持格式 | PDF, TXT, DOCX | 覆盖常用办公文档格式 |
| 批量处理 | 32-64 chunks/批 | 平衡速度和显存使用 |

### 5.6.2 知识库组织建议

1. **按主题组织知识库**
   - 产品手册 → 独立知识库
   - 客服手册 → 独立知识库
   - 技术文档 → 独立知识库

2. **合理命名**
   - 使用清晰的命名规范
   - 包含版本信息（如 "产品手册_v2.0"）

3. **定期维护**
   - 及时更新过期文档
   - 删除无用文档
   - 定期检查检索质量

---

## 5.7 工程化检查清单

- [ ] 文件大小限制
- [ ] 文件类型验证
- [ ] 文件去重（SHA-256）
- [ ] 文本提取错误处理
- [ ] 智能分块（段落级别）
- [ ] Overlap 配置
- [ ] 批量向量化
- [ ] 异常捕获与状态更新
- [ ] 权限检查
- [ ] 操作日志
- [ ] 进度反馈（WebSocket/SSE）
- [ ] 失败重试机制
- [ ] 监控指标（处理时间、成功率）

---

**下一章预告**：我们将深入探讨上下文窗口管理、会话状态管理、Redis 持久化以及 Token 优化策略。
