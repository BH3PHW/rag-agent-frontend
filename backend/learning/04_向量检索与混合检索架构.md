# 第4章：向量检索与混合检索架构

> 🎯 **学习目标**：深入理解向量嵌入原理、混合检索架构、ChromaDB使用，以及多知识库检索
> 💡 **学习重点**：向量相似度计算、混合检索策略、向量数据库实践

---

## 4.1 向量嵌入原理

### 4.1.1 什么是向量嵌入？

```
┌─────────────────────────────────────────────────────────────────────┐
│                    向量嵌入的核心思想                                │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  文本 → [Embedding模型] → 高维向量（如1536维）                        │
│                                                                     │
│  例子：                                                             │
│  ┌─────────────────────────────────────────────────────────┐      │
│  │                                                          │      │
│  │  "退款政策"  →  [0.12, -0.45, 0.78, ..., 0.32]            │      │
│  │  "退货流程"  →  [0.15, -0.42, 0.80, ..., 0.28]            │      │
│  │  "产品介绍"  →  [0.85, 0.12, -0.30, ..., 0.75]            │      │
│  │                                                          │      │
│  │  观察：                                                  │      │
│  │  - "退款政策"和"退货流程"向量很接近 → 语义相似            │      │
│  │  - "产品介绍"与前两者距离很远 → 语义不同                  │      │
│  │                                                          │      │
│  │  ✅ 向量空间中的距离反映了语义相似度！                     │      │
│  └─────────────────────────────────────────────────────────┘      │
│                                                                     │
│  为什么有效？                                                       │
│  ┌─────────────────────────────────────────────────────────┐      │
│  │                                                          │      │
│  │  1. 语义相似的文本在向量空间中距离近                       │      │
│  │  2. 可以用数学计算（如余弦相似度）快速比较                  │      │
│  │  3. 支持模糊匹配，不依赖精确关键词                          │      │
│  │                                                          │      │
│  └─────────────────────────────────────────────────────────┘      │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 4.1.2 相似度计算方法

```
┌─────────────────────────────────────────────────────────────────────┐
│                    三种主要相似度计算方法                            │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  1. 余弦相似度（Cosine Similarity）                                │
│  ┌─────────────────────────────────────────────────────────┐      │
│  │                                                          │      │
│  │         A · B                                            │      │
│  │  cos = ─────────                                         │      │
│  │         |A| × |B|                                         │      │
│  │                                                          │      │
│  │  取值范围：[-1, 1]                                        │      │
│  │  - 1：完全相同方向                                       │      │
│  │  - 0：垂直（无关联）                                     │      │
│  │  - -1：完全相反方向                                      │      │
│  │                                                          │      │
│  │  ✅ 优点：只关心方向，不关心向量长度                       │      │
│  │  ✅ 最常用！                                               │      │
│  └─────────────────────────────────────────────────────────┘      │
│                                                                     │
│  2. 点积（Dot Product）                                            │
│  ┌─────────────────────────────────────────────────────────┐      │
│  │                                                          │      │
│  │  dot = A · B = A₁×B₁ + A₂×B₂ + ... + An×Bn              │      │
│  │                                                          │      │
│  │  取值范围：[-∞, +∞]                                       │      │
│  │                                                          │      │
│  │  ✅ 优点：计算最快                                        │      │
│  │  ❌ 缺点：受向量长度影响                                   │      │
│  └─────────────────────────────────────────────────────────┘      │
│                                                                     │
│  3. 欧氏距离（Euclidean Distance）                                │
│  ┌─────────────────────────────────────────────────────────┐      │
│  │                                                          │      │
│  │        ┌─────────────────────────────                    │      │
│  │  d =  \│ Σ(Ai - Bi)²                                     │      │
│  │                                                          │      │
│  │  取值范围：[0, +∞]                                        │      │
│  │  - 0：完全相同                                           │      │
│  │  - 越大越不相似                                          │      │
│  │                                                          │      │
│  │  ✅ 优点：物理意义直观                                    │      │
│  │  ❌ 缺点：高维空间效果不如余弦相似度                       │      │
│  └─────────────────────────────────────────────────────────┘      │
│                                                                     │
│  本项目使用：余弦相似度！                                            │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 4.1.3 嵌入模型代码实现

**文件位置**：`backend/knowledge-service/embeddings.py`

```python
from typing import List, Optional
from sentence_transformers import SentenceTransformer
from common.config import settings


class EmbeddingModel:
    """Embedding model wrapper"""
    
    def __init__(self, model_name: str = "shibing624/text2vec-base-chinese"):
        self.model_name = model_name
        self._model = None
    
    @property
    def model(self):
        """Lazy load model"""
        if self._model is None:
            self._model = SentenceTransformer(self.model_name)
        return self._model
    
    def encode(self, texts: List[str], normalize: bool = True) -> List[List[float]]:
        """Generate embeddings for texts"""
        embeddings = self.model.encode(texts, normalize_embeddings=normalize)
        return embeddings.tolist()
    
    def get_dimension(self) -> int:
        """Get embedding dimension"""
        return self.model.get_sentence_embedding_dimension()


_embedding_model: Optional[EmbeddingModel] = None


def get_embedding_model() -> EmbeddingModel:
    """Get or create embedding model singleton"""
    global _embedding_model
    if _embedding_model is None:
        _embedding_model = EmbeddingModel()
    return _embedding_model


def generate_embeddings(texts: List[str]) -> List[List[float]]:
    """Generate embeddings for a list of texts"""
    model = get_embedding_model()
    return model.encode(texts)
```

**工程化亮点**：
1. **懒加载**：模型只在第一次使用时加载，节省内存
2. **单例模式**：全局只有一个模型实例，避免重复加载
3. **归一化**：默认对向量进行归一化，便于余弦相似度计算
4. **中文优化**：使用 `shibing624/text2vec-base-chinese` 模型，专门针对中文优化

---

## 4.2 ChromaDB 向量数据库

### 4.2.1 ChromaDB 简介

```
┌─────────────────────────────────────────────────────────────────────┐
│                    ChromaDB 核心概念                                │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  Collection（集合）：                                                │
│  ┌─────────────────────────────────────────────────────────┐      │
│  │  类似数据库的表，存储向量和元数据                         │      │
│  │  本项目：每个企业一个 collection                          │      │
│  └─────────────────────────────────────────────────────────┘      │
│                                                                     │
│  Document（文档）：                                                  │
│  ┌─────────────────────────────────────────────────────────┐      │
│  │  原始文本内容（如文档片段）                              │      │
│  └─────────────────────────────────────────────────────────┘      │
│                                                                     │
│  Embedding（向量）：                                                 │
│  ┌─────────────────────────────────────────────────────────┐      │
│  │  文档对应的高维向量                                       │      │
│  └─────────────────────────────────────────────────────────┘      │
│                                                                     │
│  Metadata（元数据）：                                                │
│  ┌─────────────────────────────────────────────────────────┐      │
│  │  额外信息，如文件名、页码、知识库ID等                     │      │
│  └─────────────────────────────────────────────────────────┘      │
│                                                                     │
│  ID（标识符）：                                                      │
│  ┌─────────────────────────────────────────────────────────┐      │
│  │  每个文档的唯一标识                                       │      │
│  └─────────────────────────────────────────────────────────┘      │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 4.2.2 VectorStore 代码实现

**文件位置**：`backend/knowledge-service/vector_store.py`

```python
from typing import List, Dict, Any, Optional
import chromadb
from chromadb.config import Settings
from common.config import settings
from knowledge_service.embeddings import generate_embeddings


class VectorStore:
    """ChromaDB vector store manager"""
    
    def __init__(self):
        self.client = chromadb.Client(Settings(
            chroma_db_impl="duckdb+parquet",
            persist_directory=".chroma_db"
        ))
    
    def get_collection(self, enterprise_id: str):
        """Get or create collection for enterprise"""
        collection_name = f"enterprise_{enterprise_id}"
        
        try:
            return self.client.get_collection(name=collection_name)
        except:
            return self.client.create_collection(
                name=collection_name,
                metadata={"enterprise_id": enterprise_id}
            )
```

**设计要点**：
- **持久化存储**：使用 `duckdb+parquet` 存储引擎，数据持久化到磁盘
- **租户隔离**：每个企业一个独立的 collection，数据完全隔离
- **自动创建**：collection 不存在时自动创建，无需手动初始化

```python
    def add_chunks(
        self,
        enterprise_id: str,
        knowledge_base_id: str,
        chunks: List[Dict[str, Any]]
    ) -> bool:
        """Add chunks to vector store"""
        if not chunks:
            return True
        
        collection = self.get_collection(enterprise_id)
        
        ids = [f"{knowledge_base_id}_{c['chunk_id']}" for c in chunks]
        documents = [c["content"] for c in chunks]
        embeddings = generate_embeddings(documents)
        metadatas = [
            {
                "knowledge_base_id": knowledge_base_id,
                **c.get("metadata", {})
            }
            for c in chunks
        ]
        
        collection.add(
            ids=ids,
            embeddings=embeddings,
            documents=documents,
            metadatas=metadatas
        )
        
        return True
```

**关键设计**：
1. **ID 生成**：`{knowledge_base_id}_{chunk_id}`，确保全局唯一且可追溯
2. **元数据**：包含 `knowledge_base_id`，支持按知识库过滤
3. **批量添加**：一次调用添加多个 chunk，提高效率

```python
    def similarity_search(
        self,
        enterprise_id: str,
        query: str,
        knowledge_base_id: Optional[str] = None,
        top_k: int = None
    ) -> List[Dict[str, Any]]:
        """Search similar chunks"""
        collection = self.get_collection(enterprise_id)
        
        try:
            where_filter = {"knowledge_base_id": knowledge_base_id} if knowledge_base_id else None
            
            results = collection.query(
                query_embeddings=generate_embeddings([query]),
                n_results=top_k or settings.TOP_K,
                where=where_filter
            )
            
            chunks = []
            if results and "documents" in results and len(results["documents"]) > 0:
                for i, doc in enumerate(results["documents"][0]):
                    chunk = {
                        "content": doc,
                        "metadata": results["metadatas"][0][i] if "metadatas" in results and i < len(results["metadatas"][0]) else {},
                        "distance": results["distances"][0][i] if "distances" in results and i < len(results["distances"][0]) else None
                    }
                    chunks.append(chunk)
            
            return chunks
        except Exception as e:
            print(f"Vector search error: {e}")
            return []
```

**功能说明**：
- **过滤支持**：通过 `where_filter` 支持按知识库 ID 过滤
- **Top-K**：返回最相关的 K 个结果
- **异常处理**：搜索失败时返回空列表，不影响主流程
- **结果格式化**：将 ChromaDB 的原始结果格式化为统一的 chunk 结构

```python
    def delete_chunks(self, enterprise_id: str, knowledge_base_id: str) -> bool:
        """Delete all chunks for a knowledge base"""
        collection = self.get_collection(enterprise_id)
        
        try:
            collection.delete(where={"knowledge_base_id": knowledge_base_id})
            return True
        except Exception as e:
            print(f"Delete chunks error: {e}")
            return False
    
    def delete_document(self, enterprise_id: str, document_id: str) -> bool:
        """Delete chunks for a specific document"""
        collection = self.get_collection(enterprise_id)
        
        try:
            collection.delete(where={"document_id": document_id})
            return True
        except Exception as e:
            print(f"Delete document error: {e}")
            return False


_vector_store: Optional[VectorStore] = None


def get_vector_store() -> VectorStore:
    """Get or create vector store singleton"""
    global _vector_store
    if _vector_store is None:
        _vector_store = VectorStore()
    return _vector_store
```

---

## 4.3 混合检索架构

### 4.3.1 为什么需要混合检索？

```
┌─────────────────────────────────────────────────────────────────────┐
│                    单一检索方式的局限性                              │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  关键词检索（BM25）：                                                │
│  ┌─────────────────────────────────────────────────────────┐      │
│  │  ✅ 精确匹配好，如"退货政策"                              │      │
│  │  ✅ 计算快，成本低                                        │      │
│  │  ❌ 不理解语义，如"退款"和"退货"可能匹配不到               │      │
│  │  ❌ 对拼写错误、同义词不友好                               │      │
│  └─────────────────────────────────────────────────────────┘      │
│                                                                     │
│  向量检索（Embedding）：                                             │
│  ┌─────────────────────────────────────────────────────────┐      │
│  │  ✅ 理解语义，"退款"和"退货"能匹配                        │      │
│  │  ✅ 对同义词、拼写错误鲁棒                                │      │
│  │  ❌ 计算较慢，成本较高                                    │      │
│  │  ❌ 对精确匹配可能不如关键词检索                           │      │
│  └─────────────────────────────────────────────────────────┘      │
│                                                                     │
│  最佳方案：混合检索！                                                │
│  ┌─────────────────────────────────────────────────────────┐      │
│  │  结合两者的优点，弥补各自的缺点                           │      │
│  └─────────────────────────────────────────────────────────┘      │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 4.3.2 混合检索策略

```
┌─────────────────────────────────────────────────────────────────────┐
│                    本项目的混合检索策略                              │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│                           用户问题                                   │
│                              │                                       │
│                              ▼                                       │
│              ┌───────────────────────────────┐                      │
│              │   1. FAQ 精确匹配优先         │                      │
│              │   - PostgreSQL 关键词搜索     │                      │
│              │   - 匹配度 ≥ 0.85 → 直接返回  │                      │
│              └───────────────┬───────────────┘                      │
│                              │                                       │
│                      匹配成功？│                                       │
│                   ┌──────────┴──────────┐                           │
│                   │ 是                 │ 否                        │
│                   ▼                    ▼                           │
│         ┌───────────────┐   ┌───────────────────────┐              │
│         │  返回 FAQ 答案 │   │  2. 向量检索          │              │
│         └───────────────┘   │  - 问题向量化         │              │
│                              │  - ChromaDB 搜索      │              │
│                              │  - Top-K = 20        │              │
│                              └───────────┬───────────┘              │
│                                          │                          │
│                                          ▼                          │
│                              ┌───────────────────────┐              │
│                              │  3. 重排序           │              │
│                              │  - 交叉编码器         │              │
│                              │  - 返回 Top-3         │              │
│                              └───────────┬───────────┘              │
│                                          │                          │
│                                          ▼                          │
│                              ┌───────────────────────┐              │
│                              │  4. LLM 生成答案      │              │
│                              └───────────────────────┘              │
│                                                                     │
│  特点：                                                             │
│  ✅ FAQ 优先：快速响应常见问题                                      │
│  ✅ 向量兜底：复杂问题也能找到相关内容                              │
│  ✅ 重排序优化：提高检索质量                                        │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 4.3.3 其他混合检索策略

```
┌─────────────────────────────────────────────────────────────────────┐
│                    常见的混合检索策略对比                            │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  策略1：并行召回 + 分数融合                                         │
│  ┌─────────────────────────────────────────────────────────┐      │
│  │                                                          │      │
│  │  用户问题                                                │      │
│  │      ├─────────→ 关键词检索 → 结果列表A                   │      │
│  │      │                                                    │      │
│  │      └─────────→ 向量检索 → 结果列表B                     │      │
│  │                                                          │      │
│  │  融合方式：                                               │      │
│  │  - RRF（Reciprocal Rank Fusion）                          │      │
│  │  - 线性加权融合                                          │      │
│  │                                                          │      │
│  │  最终结果：融合排序后的 Top-K                             │      │
│  │                                                          │      │
│  └─────────────────────────────────────────────────────────┘      │
│                                                                     │
│  策略2：两阶段检索（粗排 + 精排）                                    │
│  ┌─────────────────────────────────────────────────────────┐      │
│  │                                                          │      │
│  │  阶段1：关键词检索 → 快速召回 Top-100                    │      │
│  │  阶段2：对这100个结果做向量重排序 → 返回 Top-10          │      │
│  │                                                          │      │
│  │  优点：兼顾速度和精度                                     │      │
│  └─────────────────────────────────────────────────────────┘      │
│                                                                     │
│  策略3：问题分类 + 路由                                              │
│  ┌─────────────────────────────────────────────────────────┐      │
│  │                                                          │      │
│  │  先对用户问题分类：                                       │      │
│  │  - 简单事实类 → FAQ/关键词检索                           │      │
│  │  - 复杂推理类 → 向量检索 + RAG                           │      │
│  │                                                          │      │
│  │  优点：针对不同问题类型使用最优策略                       │      │
│  └─────────────────────────────────────────────────────────┘      │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 4.4 多知识库检索

### 4.4.1 多知识库架构

```
┌─────────────────────────────────────────────────────────────────────┐
│                    多知识库设计                                      │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  企业A                                                              │
│  ┌─────────────────────────────────────────────────────────┐      │
│  │                                                          │      │
│  │  Collection: enterprise_A                                │      │
│  │                                                          │      │
│  │  知识库1：产品手册                                        │      │
│  │  ├─ 文档1: 产品介绍.pdf                                  │      │
│  │  ├─ 文档2: 技术规格.pdf                                  │      │
│  │                                                          │      │
│  │  知识库2：客服手册                                        │      │
│  │  ├─ 文档1: 常见问题.pdf                                  │      │
│  │  ├─ 文档2: 退款流程.pdf                                  │      │
│  │                                                          │      │
│  │  知识库3：促销活动                                        │      │
│  │  ├─ 文档1: 618活动.pdf                                   │      │
│  │                                                          │      │
│  └─────────────────────────────────────────────────────────┘      │
│                                                                     │
│  检索方式：                                                         │
│  1. 跨所有知识库检索（默认）                                        │
│  2. 指定知识库检索（如只在"客服手册"中搜索）                         │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 4.4.2 数据模型

**文件位置**：`backend/knowledge-service/models.py`

```python
from datetime import datetime
from sqlalchemy import Column, String, Boolean, DateTime, ForeignKey, Text, Integer, BigInteger
from sqlalchemy.dialects.postgresql import UUID, JSONB
import uuid

from .database import Base


class KnowledgeBase(Base):
    """Knowledge base model"""
    __tablename__ = "knowledge_bases"
    
    id = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    enterprise_id = Column(UUID(as_uuid=True), ForeignKey("enterprises.id"), nullable=False)
    name = Column(String(100), nullable=False)
    description = Column(Text)
    is_active = Column(Boolean, default=True)
    document_count = Column(Integer, default=0)
    chunk_count = Column(Integer, default=0)
    metadata_ = Column("metadata", JSONB, default={})
    created_at = Column(DateTime, default=datetime.utcnow)
    updated_at = Column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)
    
    def __repr__(self):
        return f"<KnowledgeBase(id={self.id}, name={self.name})>"


class Document(Base):
    """Document model"""
    __tablename__ = "documents"
    
    id = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    knowledge_base_id = Column(UUID(as_uuid=True), ForeignKey("knowledge_bases.id"), nullable=False)
    filename = Column(String(255), nullable=False)
    file_path = Column(String(500))
    file_size = Column(BigInteger)
    file_type = Column(String(20))
    file_hash = Column(String(64))
    chunk_count = Column(Integer, default=0)
    status = Column(String(20), default="pending")  # pending, processing, completed, failed
    error_message = Column(Text)
    metadata_ = Column("metadata", JSONB, default={})
    created_at = Column(DateTime, default=datetime.utcnow)
    updated_at = Column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)
    
    def __repr__(self):
        return f"<Document(id={self.id}, filename={self.filename})>"
```

**设计要点**：
1. **租户隔离**：通过 `enterprise_id` 实现企业级数据隔离
2. **层级结构**：企业 → 知识库 → 文档 → 片段
3. **状态管理**：文档有 `pending`/`processing`/`completed`/`failed` 状态
4. **统计信息**：知识库有 `document_count` 和 `chunk_count`，方便展示

---

## 4.5 检索优化技巧

### 4.5.1 Chunk 大小优化

```
┌─────────────────────────────────────────────────────────────────────┐
│                    Chunk 大小对检索质量的影响                        │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  Chunk 太小：                                                        │
│  ┌─────────────────────────────────────────────────────────┐      │
│  │  ✅ 检索更精准                                            │      │
│  │  ❌ 上下文不完整，LLM 难以理解                            │      │
│  │  ❌ 片段数量太多，管理成本高                              │      │
│  └─────────────────────────────────────────────────────────┘      │
│                                                                     │
│  Chunk 太大：                                                        │
│  ┌─────────────────────────────────────────────────────────┐      │
│  │  ✅ 上下文完整                                            │      │
│  │  ❌ 检索不够精准，可能包含无关内容                         │      │
│  │  ❌ 超过 LLM 上下文窗口限制                               │      │
│  └─────────────────────────────────────────────────────────┘      │
│                                                                     │
│  推荐配置：                                                         │
│  ┌─────────────────────────────────────────────────────────┐      │
│  │                                                          │      │
│  │  Chunk 大小：500-1000 字符（中文）                       │      │
│  │  Overlap：50-100 字符（前后重叠）                        │      │
│  │                                                          │      │
│  │  为什么 overlap？                                        │      │
│  │  - 避免语义被截断                                        │      │
│  │  - 提高检索的召回率                                      │      │
│  │                                                          │      │
│  └─────────────────────────────────────────────────────────┘      │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 4.5.2 分块策略代码

**文件位置**：`backend/knowledge-service/chunker.py`

```python
from typing import List, Dict, Any
import re
from common.config import settings


class TextChunker:
    """Text chunking for RAG"""
    
    def __init__(
        self,
        chunk_size: int = None,
        chunk_overlap: int = None,
        separator: str = "\n"
    ):
        self.chunk_size = chunk_size or settings.CHUNK_SIZE
        self.chunk_overlap = chunk_overlap or settings.CHUNK_OVERLAP
        self.separator = separator
    
    def chunk_text(self, text: str, metadata: Dict[str, Any] = None) -> List[Dict[str, Any]]:
        """Split text into chunks"""
        chunks = []
        
        # Split by separator
        sentences = re.split(self.separator, text)
        sentences = [s.strip() for s in sentences if s.strip()]
        
        current_chunk = ""
        chunk_id = 0
        
        for sentence in sentences:
            # Check if adding this sentence exceeds chunk size
            if len(current_chunk) + len(sentence) + 1 <= self.chunk_size:
                current_chunk += sentence + " "
            else:
                # Save current chunk if not empty
                if current_chunk.strip():
                    chunks.append({
                        "content": current_chunk.strip(),
                        "chunk_id": str(chunk_id),
                        "metadata": metadata or {}
                    })
                    chunk_id += 1
                
                # Start new chunk with overlap
                overlap_text = current_chunk[-self.chunk_overlap:] if len(current_chunk) > self.chunk_overlap else current_chunk
                current_chunk = overlap_text + sentence + " "
        
        # Don't forget the last chunk
        if current_chunk.strip():
            chunks.append({
                "content": current_chunk.strip(),
                "chunk_id": str(chunk_id),
                "metadata": metadata or {}
            })
        
        return chunks
```

**优化策略**：
1. **按分隔符分割**：先用换行符分割成句子/段落
2. **贪心合并**：将小片段合并到 chunk_size 大小
3. **重叠处理**：新 chunk 包含前一个 chunk 的尾部 overlap

### 4.5.3 其他优化技巧

| 优化项 | 说明 | 效果 |
|--------|------|------|
| 查询重写 | 用 LLM 优化用户问题表述 | 提高检索召回率 |
| 混合检索 | 结合关键词和向量检索 | 兼顾精度和召回 |
| 重排序 | 用交叉编码器精排 | 提高检索质量 |
| 元数据过滤 | 按知识库、文档等过滤 | 减少无关结果 |
| 缓存 | 缓存高频查询结果 | 提高响应速度 |
| 批量处理 | 批量向量化、批量检索 | 提高吞吐量 |

---

## 4.6 工程化最佳实践

### 4.6.1 向量数据库选型对比

| 数据库 | 特点 | 适用场景 |
|--------|------|----------|
| ChromaDB | 轻量级、易部署、Python友好 | 中小规模、快速原型 |
| Pinecone | 托管服务、可扩展、高性能 | 大规模生产环境 |
| Weaviate | 开源、支持多种过滤 | 需要复杂查询场景 |
| Milvus | 高性能、云原生 | 大规模向量检索 |
| PGVector | PostgreSQL 扩展 | 已有 PG 基础设施 |

### 4.6.2 监控与运维

1. **性能监控**：
   - 检索耗时（P50/P95/P99）
   - 向量数据库 QPS
   - 模型推理耗时

2. **质量监控**：
   - 检索相关性（人工标注）
   - 用户满意度反馈
   - 坏案例收集与分析

3. **容量规划**：
   - 向量数量增长监控
   - 存储空间监控
   - 内存使用监控

---

## 4.7 检查清单

- [ ] 向量模型支持中文优化
- [ ] 每个企业有独立的 collection
- [ ] Chunk 大小配置合理（500-1000 字符）
- [ ] 有 overlap 避免语义截断
- [ ] 支持按知识库过滤检索
- [ ] 有完善的异常处理
- [ ] 有性能监控指标
- [ ] 有数据备份机制

---

**下一章预告**：我们将深入探讨知识库管理系统、文档上传处理流程、数据分块策略等内容。
