# 第6章：上下文窗口与对话管理

> 🎯 **学习目标**：深入理解会话状态管理、上下文压缩与摘要、窗口管理策略、Redis 持久化以及 Token 优化
> 💡 **学习重点**：高效管理对话上下文、优化 LLM Token 使用

---

## 6.1 对话管理系统架构

### 6.1.1 整体架构

```
┌─────────────────────────────────────────────────────────────────────┐
│                    对话管理系统架构                                  │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  用户请求                                                           │
│      │                                                              │
│      ▼                                                              │
│  ┌─────────────────────────────────────────────────────────────┐  │
│  │  Chat Service                                               │  │
│  │  ┌───────────────────────────────────────────────────────┐  │  │
│  │  │  1. 会话初始化                                         │  │  │
│  │  │  - 创建/获取 session_id                               │  │  │
│  │  │  - 从 Redis 加载上下文                                 │  │  │
│  │  └───────────────────────┬───────────────────────────────┘  │  │
│  │                          │                                    │  │
│  │  ┌───────────────────────▼───────────────────────────────┐  │  │
│  │  │  2. 上下文管理                                         │  │  │
│  │  │  - 添加新消息到上下文                                  │  │  │
│  │  │  - 检查上下文长度                                      │  │  │
│  │  │  - 触发摘要（如需要）                                  │  │  │
│  └───────────────────────┬───────────────────────────────┘  │  │
│                          │                                    │  │
│  ┌───────────────────────▼───────────────────────────────┐  │  │
│  │  3. 构建 LLM Prompt                                     │  │  │
│  │  - 系统提示词                                           │  │  │
│  │  - 会话摘要（如存在）                                   │  │  │
│  │  - 最近的对话历史                                       │  │  │
│  │  - RAG 检索结果                                         │  │  │
│  │  - 当前用户问题                                         │  │  │
│  └───────────────────────┬───────────────────────────────┘  │  │
│                          │                                    │  │
│  ┌───────────────────────▼───────────────────────────────┐  │  │
│  │  4. 调用 LLM                                           │  │  │
│  │  - 流式输出                                             │  │  │
│  │  - 超时控制                                             │  │  │
│  └───────────────────────┬───────────────────────────────┘  │  │
│                          │                                    │  │
│  ┌───────────────────────▼───────────────────────────────┐  │  │
│  │  5. 保存上下文                                         │  │  │
│  │  - 添加助手回复到上下文                                │  │  │
│  │  - 持久化到 Redis                                     │  │  │
│  │  - 更新会话状态                                        │  │  │
│  └───────────────────────────────────────────────────────┘  │  │
│  └─────────────────────────────────────────────────────────────┘  │
│                         │                                          │
│                         ▼                                          │
│  ┌─────────────────────────────────────────────────────────────┐  │
│  │  Redis                                                     │  │
│  │  - 会话上下文存储（Hash）                                   │  │
│  │  - 会话摘要存储（String）                                   │  │
│  │  - TTL: 24 小时                                            │  │
│  └─────────────────────────────────────────────────────────────┘  │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 6.1.2 数据结构设计

```
┌─────────────────────────────────────────────────────────────────────┐
│                    Redis 数据结构                                    │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  1. 会话消息列表（List）                                             │
│  ┌─────────────────────────────────────────────────────────┐      │
│  │  Key: ctx:{session_id}:messages                        │      │
│  │                                                          │      │
│  │  Value: JSON Array                                     │      │
│  │  [                                                      │      │
│  │    {                                                    │      │
│  │      "role": "user",                                    │      │
│  │      "content": "你们的营业时间是什么？",               │      │
│  │      "timestamp": "2024-01-01T10:00:00Z"                │      │
│  │    },                                                   │      │
│  │    {                                                    │      │
│  │      "role": "assistant",                               │      │
│  │      "content": "我们的营业时间是周一至周五 9:00-18:00",│      │
│  │      "timestamp": "2024-01-01T10:00:01Z"                │      │
│  │    }                                                    │      │
│  │  ]                                                      │      │
│  │                                                          │      │
│  │  TTL: 86400 (24 小时)                                   │      │
│  └─────────────────────────────────────────────────────────┘      │
│                                                                     │
│  2. 会话摘要（String）                                               │
│  ┌─────────────────────────────────────────────────────────┐      │
│  │  Key: ctx:{session_id}:summary                          │      │
│  │                                                          │      │
│  │  Value: "用户询问了营业时间，已告知周一至周五 9:00-18:00"│      │
│  │                                                          │      │
│  │  TTL: 86400 (24 小时)                                   │      │
│  └─────────────────────────────────────────────────────────┘      │
│                                                                     │
│  3. 会话元数据（Hash）                                               │
│  ┌─────────────────────────────────────────────────────────┐      │
│  │  Key: ctx:{session_id}:meta                             │      │
│  │                                                          │      │
│  │  Fields:                                               │      │
│  │  - user_id: "user-123"                                 │      │
│  │  - enterprise_id: "ent-456"                            │      │
│  │  - created_at: "2024-01-01T10:00:00Z"                 │      │
│  │  - updated_at: "2024-01-01T10:05:00Z"                 │      │
│  │  - message_count: "10"                                 │      │
│  │                                                          │      │
│  │  TTL: 86400 (24 小时)                                   │      │
│  └─────────────────────────────────────────────────────────┘      │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 6.2 核心代码实现

### 6.2.1 会话上下文类

**文件位置**：`backend/chat-service/context_manager.py`

```python
from typing import List, Dict, Any, Optional, Tuple
from uuid import UUID
from datetime import datetime
import json

from .redis_client import redis_manager


class ConversationContext:
    """
    Data structure for conversation context
    
    Stores:
    - Session ID
    - Message history
    - Conversation summary
    - Last updated timestamp
    """
    
    def __init__(
        self,
        session_id: UUID,
        messages: List[Dict[str, Any]] = None,
        summary: str = None,
        last_update: datetime = None
    ):
        self.session_id = session_id
        self.messages = messages or []
        self.summary = summary
        self.last_update = last_update or datetime.utcnow()
    
    def to_dict(self) -> Dict[str, Any]:
        """Convert to dictionary for serialization"""
        return {
            "session_id": str(self.session_id),
            "messages": self.messages,
            "summary": self.summary,
            "last_update": self.last_update.isoformat(),
            "message_count": len(self.messages)
        }
    
    @classmethod
    def from_dict(cls, data: Dict[str, Any]) -> "ConversationContext":
        """Create from dictionary"""
        return cls(
            session_id=UUID(data["session_id"]),
            messages=data.get("messages", []),
            summary=data.get("summary"),
            last_update=datetime.fromisoformat(data["last_update"])
        )
    
    def add_message(
        self,
        role: str,
        content: str,
        metadata: Dict[str, Any] = None
    ):
        """
        Add a message to the conversation
        
        Args:
            role: "user" or "assistant"
            content: Message content
            metadata: Optional metadata
        """
        message = {
            "role": role,
            "content": content,
            "timestamp": datetime.utcnow().isoformat()
        }
        if metadata:
            message["metadata"] = metadata
        
        self.messages.append(message)
        self.last_update = datetime.utcnow()
    
    def get_recent_messages(self, limit: int = 10) -> List[Dict[str, Any]]:
        """Get the most recent N messages"""
        return self.messages[-limit:]
```

### 6.2.2 上下文管理器类

```python
class ContextWindowManager:
    """
    Manages conversation context windows with Redis persistence
    
    Features:
    - Stateless conversation storage in Redis
    - Automatic summarization for long conversations
    - Session lifecycle management
    - Token-aware context window management
    """
    
    # Redis key prefixes
    CONTEXT_PREFIX = "ctx:"
    SUMMARY_PREFIX = "sum:"
    META_PREFIX = "meta:"
    
    # Configuration
    MAX_MESSAGES = 50  # Maximum messages to keep in history
    SUMMARIZE_THRESHOLD = 20  # Trigger summarization after N messages
    KEEP_RECENT_TURNS = 5  # Keep recent N turns unchanged when summarizing
    SESSION_TTL = 86400  # 24 hours in seconds
    
    def __init__(self):
        self.redis = redis_manager
        self.summarizer = ContextSummarizer()
    
    async def get_context(self, session_id: UUID) -> ConversationContext:
        """
        Get conversation context from Redis
        
        Args:
            session_id: Session ID
            
        Returns:
            ConversationContext object
        """
        context_key = f"{self.CONTEXT_PREFIX}{session_id}"
        summary_key = f"{self.SUMMARY_PREFIX}{session_id}"
        
        # Try to get from Redis
        context_data = await self.redis.get(context_key)
        summary = await self.redis.get(summary_key)
        
        if context_data:
            context_dict = json.loads(context_data.decode('utf-8'))
            messages = context_dict.get("messages", [])
        else:
            messages = []
        
        return ConversationContext(
            session_id=session_id,
            messages=messages,
            summary=summary.decode('utf-8') if summary else None
        )
    
    async def add_message(
        self,
        session_id: UUID,
        role: str,
        content: str,
        metadata: Dict[str, Any] = None
    ) -> ConversationContext:
        """
        Add a message to the conversation context
        
        Args:
            session_id: Session ID
            role: "user" or "assistant"
            content: Message content
            metadata: Optional metadata
            
        Returns:
            Updated ConversationContext
        """
        context = await self.get_context(session_id)
        
        # Add new message
        context.add_message(role, content, metadata)
        
        # Trim messages if too many
        if len(context.messages) > self.MAX_MESSAGES:
            context.messages = context.messages[-self.MAX_MESSAGES:]
        
        # Check if summarization is needed
        if len(context.messages) >= self.SUMMARIZE_THRESHOLD:
            summary, remaining = await self.summarizer.summarize(
                context.messages,
                keep_recent_turns=self.KEEP_RECENT_TURNS
            )
            context.summary = summary
            context.messages = remaining
        
        # Save to Redis
        await self._save_context(context)
        
        return context
    
    async def get_messages_for_llm(
        self,
        session_id: UUID,
        max_turns: int = 10
    ) -> List[Dict[str, Any]]:
        """
        Get messages formatted for LLM context
        
        Includes summary if available
        
        Args:
            session_id: Session ID
            max_turns: Maximum number of turns to return
            
        Returns:
            List of messages suitable for LLM
        """
        context = await self.get_context(session_id)
        
        messages = []
        
        # Add summary if exists
        if context.summary:
            messages.append({
                "role": "system",
                "content": f"【会话历史摘要】\n{context.summary}\n\n以下是最近的对话细节："
            })
        
        # Add recent messages
        recent_messages = context.get_recent_messages(max_turns * 2)
        messages.extend(recent_messages)
        
        return messages
    
    async def _save_context(self, context: ConversationContext):
        """
        Save context to Redis
        
        Args:
            context: ConversationContext to save
        """
        context_key = f"{self.CONTEXT_PREFIX}{context.session_id}"
        summary_key = f"{self.SUMMARY_PREFIX}{context.session_id}"
        
        # Save messages
        context_data = {
            "session_id": str(context.session_id),
            "messages": context.messages,
            "last_update": context.last_update.isoformat()
        }
        await self.redis.set(
            context_key,
            json.dumps(context_data),
            ex=self.SESSION_TTL
        )
        
        # Save summary if exists
        if context.summary:
            await self.redis.set(
                summary_key,
                context.summary,
                ex=self.SESSION_TTL
            )
        else:
            await self.redis.delete(summary_key)
    
    async def clear_context(self, session_id: UUID) -> bool:
        """
        Clear conversation context
        
        Args:
            session_id: Session ID
            
        Returns:
            True if cleared
        """
        context_key = f"{self.CONTEXT_PREFIX}{session_id}"
        summary_key = f"{self.SUMMARY_PREFIX}{session_id}"
        meta_key = f"{self.META_PREFIX}{session_id}"
        
        await self.redis.delete(context_key, summary_key, meta_key)
        return True
    
    async def extend_session_ttl(self, session_id: UUID) -> bool:
        """
        Extend session TTL
        
        Args:
            session_id: Session ID
            
        Returns:
            True if extended
        """
        context_key = f"{self.CONTEXT_PREFIX}{session_id}"
        await self.redis.expire(context_key, self.SESSION_TTL)
        return True
    
    async def get_session_info(self, session_id: UUID) -> Dict[str, Any]:
        """
        Get session information
        
        Args:
            session_id: Session ID
            
        Returns:
            Session info dict
        """
        context = await self.get_context(session_id)
        
        return {
            "session_id": str(session_id),
            "message_count": len(context.messages),
            "has_summary": context.summary is not None,
            "last_update": context.last_update.isoformat()
        }
```

### 6.2.3 上下文摘要器

```python
class ContextSummarizer:
    """
    Summarizer for conversation history
    
    Uses lightweight processing to summarize old conversation turns
    Can be extended to use LLM for better summaries
    """
    
    MAX_CHARS_PER_SUMMARY = 500
    SUMMARY_TEMPLATE = "会话摘要: {summary}\n\n"
    
    def __init__(self):
        self.summary_prompt = """请简要总结以下对话的核心内容和关键信息：

{conversation}

请用简洁的语言概括对话的主要话题、用户需求和已讨论的关键点。"""
    
    async def summarize(
        self,
        messages: List[Dict[str, Any]],
        keep_recent_turns: int = 5
    ) -> Tuple[str, List[Dict[str, Any]]]:
        """
        Summarize conversation history
        
        Args:
            messages: List of message dicts
            keep_recent_turns: Number of recent turns to keep unchanged
            
        Returns:
            Tuple of (summary, remaining_messages)
        """
        if len(messages) <= keep_recent_turns * 2:
            return "", messages
        
        # Separate messages: old vs recent
        recent_messages = messages[-(keep_recent_turns * 2):]
        old_messages = messages[:-(keep_recent_turns * 2)]
        
        # Build conversation text for summarization
        conversation_text = self._format_conversation(old_messages)
        
        # Generate summary
        # Note: In production, this would call a small/fast LLM
        summary = await self._generate_summary(conversation_text)
        
        return summary, recent_messages
    
    def _format_conversation(self, messages: List[Dict[str, Any]]) -> str:
        """Format messages for summarization prompt"""
        lines = []
        for msg in messages:
            role = msg.get("role", "user")
            content = msg.get("content", "")
            # Truncate long messages
            if len(content) > 200:
                content = content[:200] + "..."
            if role == "user":
                lines.append(f"用户: {content}")
            else:
                lines.append(f"助手: {content}")
        return "\n".join(lines)
    
    async def _generate_summary(self, conversation_text: str) -> str:
        """
        Generate summary using lightweight method
        
        In production, this would call a small LLM
        
        Args:
            conversation_text: Formatted conversation
            
        Returns:
            Summary text
        """
        if not conversation_text.strip():
            return ""
        
        # Simple extractive summarization (fallback)
        # Extract key topics mentioned
        keywords = []
        topic_indicators = ["关于", "问题", "咨询", "询问", "查询", "订单", "产品", "退款", "投诉", "退换货", "客服"]
        
        for indicator in topic_indicators:
            if indicator in conversation_text:
                keywords.append(indicator)
        
        # Get first and last exchanges
        lines = conversation_text.split("\n")
        summary_parts = []
        
        if lines:
            summary_parts.append(f"首轮: {lines[0]}")
        if len(lines) > 2:
            summary_parts.append(f"末轮: {lines[-1]}")
        
        if keywords:
            summary_parts.append(f"涉及话题: {', '.join(set(keywords))}")
        
        summary = f"对话涉及{len(lines)//2}轮交流。" + "。".join(summary_parts)
        
        if len(summary) > self.MAX_CHARS_PER_SUMMARY:
            summary = summary[:self.MAX_CHARS_PER_SUMMARY] + "..."
        
        return summary
    
    async def generate_summary_with_llm(
        self,
        conversation_text: str,
        llm_client: Any
    ) -> str:
        """
        Generate summary using LLM (for production use)
        
        Args:
            conversation_text: Formatted conversation
            llm_client: LLM client instance
            
        Returns:
            LLM-generated summary
        """
        prompt = self.summary_prompt.format(conversation=conversation_text)
        
        try:
            summary = await llm_client.generate(
                prompt=prompt,
                max_tokens=200,
                temperature=0.3
            )
            return summary.strip()
        except Exception as e:
            print(f"LLM summarization failed: {e}, using fallback")
            return await self._generate_summary(conversation_text)
```

---

## 6.3 窗口管理策略

### 6.3.1 策略对比

```
┌─────────────────────────────────────────────────────────────────────┐
│                    上下文窗口管理策略对比                            │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  策略1：固定保留最近 N 条（Simple）                                  │
│  ┌─────────────────────────────────────────────────────────┐      │
│  │                                                          │      │
│  │  方法：只保留最近的 N 条消息                              │      │
│  │                                                          │      │
│  │  优点：                                                 │      │
│  │  ✅ 实现简单                                            │      │
│  │  ✅ 计算快                                              │      │
│  │                                                          │      │
│  │  缺点：                                                 │      │
│  │  ❌ 丢失早期上下文                                      │      │
│  │  ❌ 可能错过重要信息                                    │      │
│  │                                                          │      │
│  └─────────────────────────────────────────────────────────┘      │
│                                                                     │
│  策略2：摘要 + 最近 N 条（Better）                                   │
│  ┌─────────────────────────────────────────────────────────┐      │
│  │                                                          │      │
│  │  方法：                                                 │      │
│  │  1. 将早期对话压缩为摘要                                │      │
│  │  2. 保留最近的 N 条完整对话                              │      │
│  │  3. Prompt: [摘要] + [最近 N 条] + [当前问题]           │      │
│  │                                                          │      │
│  │  优点：                                                 │      │
│  │  ✅ 保留重要上下文信息                                  │      │
│  │  ✅ 节省 Token                                         │      │
│  │  ✅ LLM 能理解历史背景                                  │      │
│  │                                                          │      │
│  │  缺点：                                                 │      │
│  │  ❌ 需要额外计算摘要                                    │      │
│  │  ❌ 摘要可能丢失细节                                    │      │
│  │                                                          │      │
│  └─────────────────────────────────────────────────────────┘      │
│                                                                     │
│  策略3：智能窗口 + 相关检索（Best）                                  │
│  ┌─────────────────────────────────────────────────────────┐      │
│  │                                                          │      │
│  │  方法：                                                 │      │
│  │  1. 保留最近的 N 条对话                                 │      │
│  │  2. 根据当前问题，从历史中检索相关信息                  │      │
│  │  3. Prompt: [摘要] + [相关历史] + [最近 N 条] + [问题]  │      │
│  │                                                          │      │
│  │  优点：                                                 │      │
│  │  ✅ 最有效的上下文利用                                  │      │
│  │  ✅ 只保留相关信息                                      │      │
│  │                                                          │      │
│  │  缺点：                                                 │      │
│  │  ❌ 实现复杂                                            │      │
│  │  ❌ 需要历史消息向量化                                  │      │
│  │                                                          │      │
│  └─────────────────────────────────────────────────────────┘      │
│                                                                     │
│  本项目使用：策略2（摘要 + 最近 N 条）                               │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 6.3.2 Token 优化策略

```
┌─────────────────────────────────────────────────────────────────────┐
│                    Token 优化策略                                    │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  1. 消息修剪（Trimming）                                             │
│  ┌─────────────────────────────────────────────────────────┐      │
│  │                                                          │      │
│  │  方法：将超长消息截断到一定长度                          │      │
│  │                                                          │      │
│  │  示例：用户发送 10000 字符的消息 → 截断到 1000 字符     │      │
│  │                                                          │      │
│  │  优点：大幅节省 Token                                    │      │
│  │  缺点：可能丢失重要信息                                  │      │
│  │                                                          │      │
│  └─────────────────────────────────────────────────────────┘      │
│                                                                     │
│  2. 系统提示词优化（System Prompt）                                 │
│  ┌─────────────────────────────────────────────────────────┐      │
│  │                                                          │      │
│  │  方法：精简系统提示词，去除冗余                          │      │
│  │                                                          │      │
│  │  示例：从 500 Token 精简到 200 Token                    │      │
│  │                                                          │      │
│  │  优点：每次请求都能节省 Token                            │      │
│  │  缺点：需要仔细测试，避免影响效果                        │      │
│  │                                                          │      │
│  └─────────────────────────────────────────────────────────┘      │
│                                                                     │
│  3. RAG 结果数量控制（Top-K）                                       │
│  ┌─────────────────────────────────────────────────────────┐      │
│  │                                                          │      │
│  │  方法：只返回最相关的 Top-3 或 Top-5 个片段             │      │
│  │                                                          │      │
│  │  优点：减少上下文长度                                    │      │
│  │  缺点：可能漏掉相关信息                                  │      │
│  │                                                          │      │
│  └─────────────────────────────────────────────────────────┘      │
│                                                                     │
│  4. 消息角色合并（Merging）                                          │
│  ┌─────────────────────────────────────────────────────────┐      │
│  │                                                          │      │
│  │  方法：将连续的用户消息或助手消息合并                    │      │
│  │                                                          │      │
│  │  示例：                                                 │      │
│  │  [User: A] [User: B] → [User: A\n\nB]                  │      │
│  │                                                          │      │
│  │  优点：节省 Token（减少角色标记）                        │      │
│  │                                                          │      │
│  └─────────────────────────────────────────────────────────┘      │
│                                                                     │
│  5. Token 预算管理（Budgeting）                                     │
│  ┌─────────────────────────────────────────────────────────┐      │
│  │                                                          │      │
│  │  方法：给各部分分配 Token 预算                          │      │
│  │  - 系统提示词: 500 Token                                │      │
│  │  - 摘要: 200 Token                                      │      │
│  │  - 对话历史: 1500 Token                                 │      │
│  │  - RAG 上下文: 1000 Token                               │      │
│  │  - 当前问题: 200 Token                                  │      │
│  │  总计: 3400 Token（留余量给生成）                        │      │
│  │                                                          │      │
│  │  优点：精确控制，不会超过模型限制                        │      │
│  │                                                          │      │
│  └─────────────────────────────────────────────────────────┘      │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 6.4 Redis 持久化

### 6.4.1 Redis 配置

```python
# backend/common/config.py

class RedisConfig:
    """Redis configuration"""
    
    # Connection
    REDIS_HOST = os.getenv("REDIS_HOST", "localhost")
    REDIS_PORT = int(os.getenv("REDIS_PORT", "6379"))
    REDIS_PASSWORD = os.getenv("REDIS_PASSWORD", "")
    REDIS_DB = int(os.getenv("REDIS_DB", "0"))
    
    # Pool settings
    REDIS_MAX_CONNECTIONS = int(os.getenv("REDIS_MAX_CONNECTIONS", "50"))
    REDIS_SOCKET_TIMEOUT = int(os.getenv("REDIS_SOCKET_TIMEOUT", "5"))
    REDIS_SOCKET_CONNECT_TIMEOUT = int(os.getenv("REDIS_SOCKET_CONNECT_TIMEOUT", "5"))
    
    # Session settings
    SESSION_TTL = int(os.getenv("SESSION_TTL", "86400"))  # 24 hours
```

### 6.4.2 Redis 客户端实现

```python
# backend/chat-service/redis_client.py

import redis.asyncio as redis
from typing import Optional, Any
from common.config import settings


class RedisManager:
    """
    Async Redis client manager with connection pooling
    
    Features:
    - Connection pooling
    - Automatic reconnection
    - Graceful error handling
    """
    
    def __init__(self):
        self._client: Optional[redis.Redis] = None
        self._pool: Optional[redis.ConnectionPool] = None
    
    async def init(self):
        """Initialize Redis connection pool"""
        if self._pool is None:
            self._pool = redis.ConnectionPool(
                host=settings.REDIS_HOST,
                port=settings.REDIS_PORT,
                password=settings.REDIS_PASSWORD,
                db=settings.REDIS_DB,
                max_connections=settings.REDIS_MAX_CONNECTIONS,
                socket_timeout=settings.REDIS_SOCKET_TIMEOUT,
                socket_connect_timeout=settings.REDIS_SOCKET_CONNECT_TIMEOUT,
                retry_on_timeout=True
            )
            self._client = redis.Redis(connection_pool=self._pool)
    
    async def close(self):
        """Close Redis connection"""
        if self._client:
            await self._client.close()
        if self._pool:
            await self._pool.disconnect()
    
    @property
    def client(self) -> redis.Redis:
        """Get Redis client"""
        if self._client is None:
            raise RuntimeError("Redis manager not initialized. Call init() first.")
        return self._client
    
    async def get(self, key: str) -> Optional[bytes]:
        """Get value by key"""
        try:
            return await self.client.get(key)
        except Exception as e:
            print(f"Redis GET error: {e}")
            return None
    
    async def set(
        self,
        key: str,
        value: str | bytes,
        ex: Optional[int] = None,
        px: Optional[int] = None
    ) -> bool:
        """Set value with optional expiration"""
        try:
            await self.client.set(key, value, ex=ex, px=px)
            return True
        except Exception as e:
            print(f"Redis SET error: {e}")
            return False
    
    async def delete(self, *keys: str) -> int:
        """Delete keys"""
        try:
            return await self.client.delete(*keys)
        except Exception as e:
            print(f"Redis DELETE error: {e}")
            return 0
    
    async def expire(self, key: str, seconds: int) -> bool:
        """Set expiration on key"""
        try:
            return await self.client.expire(key, seconds)
        except Exception as e:
            print(f"Redis EXPIRE error: {e}")
            return False
    
    async def ttl(self, key: str) -> int:
        """Get TTL of key"""
        try:
            return await self.client.ttl(key)
        except Exception as e:
            print(f"Redis TTL error: {e}")
            return -1


# Singleton instance
redis_manager = RedisManager()
```

---

## 6.5 完整的 Prompt 构建流程

### 6.5.1 构建流程

```python
async def build_llm_prompt(
    context_manager: ContextWindowManager,
    session_id: UUID,
    query: str,
    rag_context: Optional[str] = None,
    max_history_turns: int = 10
) -> str:
    """
    Build complete prompt for LLM
    
    Args:
        context_manager: Context window manager
        session_id: Session ID
        query: User's current query
        rag_context: Retrieved RAG context (optional)
        max_history_turns: Maximum history turns to include
        
    Returns:
        Formatted prompt string
    """
    # 1. Get conversation context
    context = await context_manager.get_context(session_id)
    
    # 2. Build prompt parts
    prompt_parts = []
    
    # 2.1 System prompt
    system_prompt = """你是一个专业、友好、高效的智能客服助手。

核心原则：
1. 只根据提供的上下文信息回答问题，不要编造信息
2. 如果上下文信息不足，诚实告知用户
3. 回答简洁明了，避免冗长
4. 保持专业友好的语气
5. 引用来源（如有）"""
    
    prompt_parts.append(system_prompt)
    
    # 2.2 Conversation summary (if exists)
    if context.summary:
        summary_section = f"""
【对话历史摘要】
{context.summary}
"""
        prompt_parts.append(summary_section)
    
    # 2.3 Recent conversation history
    recent_messages = context.get_recent_messages(max_history_turns * 2)
    if recent_messages:
        history_section = """
【最近对话】
"""
        for msg in recent_messages:
            role = "用户" if msg["role"] == "user" else "助手"
            history_section += f"{role}: {msg['content']}\n"
        prompt_parts.append(history_section)
    
    # 2.4 RAG context (if exists)
    if rag_context:
        rag_section = f"""
【参考信息】
{rag_context}
"""
        prompt_parts.append(rag_section)
    
    # 2.5 Current query
    query_section = f"""
【当前问题】
{query}

请给出回答：
"""
    prompt_parts.append(query_section)
    
    # 3. Join all parts
    full_prompt = "\n".join(prompt_parts)
    
    return full_prompt
```

---

## 6.6 最佳实践总结

### 6.6.1 会话管理最佳实践

| 方面 | 建议值 | 说明 |
|------|--------|------|
| 会话 TTL | 24 小时 | 平衡用户体验和存储空间 |
| 摘要阈值 | 20 条消息 | 避免过早摘要，也避免上下文过长 |
| 保留最近轮数 | 5-10 轮 | 保证最近的对话完整 |
| 最大消息数 | 50 条 | 防止内存和存储浪费 |
| 单条消息长度 | 1000-2000 字符 | 避免超长消息占用过多 Token |

### 6.6.2 Token 优化检查表

- [ ] 系统提示词精简
- [ ] 对话历史摘要
- [ ] 单条消息截断
- [ ] RAG 结果数量控制（Top-3/5）
- [ ] Token 预算分配
- [ ] 冗余信息去除
- [ ] 流式输出（节省响应等待时间）

### 6.6.3 监控指标

1. **性能指标**
   - 平均会话长度
   - 平均 Token 使用量/会话
   - 摘要触发频率
   - Redis 读写延迟

2. **业务指标**
   - 会话留存率
   - 用户满意度
   - 上下文命中率（LLM 引用历史）

---

## 6.7 工程化检查清单

- [ ] Redis 连接池配置
- [ ] 会话 TTL 管理
- [ ] 上下文摘要机制
- [ ] 错误处理（Redis 不可用时的降级）
- [ ] Token 预算控制
- [ ] 消息长度限制
- [ ] 并发安全
- [ ] 监控指标
- [ ] 会话清理（定期清理过期会话）
- [ ] 数据备份（Redis 持久化配置）

---

## 总结

我们已经完成了 RAG 智能客服系统的核心学习文档：

1. **第4章** - RAG 检索与重排序优化：混合检索、Cross-Encoder 重排序、引用来源
2. **第5章** - 向量检索与混合检索架构：向量嵌入、ChromaDB、多知识库
3. **第6章** - 知识库管理与数据处理：文档处理、智能分块、权限管理
4. **第7章** - 上下文窗口与对话管理：会话管理、Token 优化、Redis 持久化

这些文档涵盖了从底层技术到工程实践的完整知识体系，可以帮助团队快速上手和二次开发。
