# 第9章：电商工具集成

> 🎯 **学习目标**：深入理解电商场景下的工具设计与实现
> 💡 **工程化思维**：多平台适配、可扩展架构、配置化管理
> 💡 **产品化思维**：用户场景、业务流程、灵活配置

---

## 9.1 电商场景概述

### 9.1.1 为什么需要电商工具？

**产品化思维**：电商客服的核心需求

```
电商客服典型场景：
┌─────────────────────────────────────────────────────────┐
│  用户："我的订单SF123456什么时候到？"              │
│  ────────────────────────────────────────────────────── │
│  传统方式：客服手动查订单 → 耗时、易出错           │
│  ────────────────────────────────────────────────────── │
│  智能方式：Agent自动调用订单查询工具 → 秒级响应    │
└─────────────────────────────────────────────────────────┘

电商工具解决的问题：
✅ 订单查询 - 自动查快递、状态、金额
✅ 商品查询 - 库存、价格、规格
✅ 多平台集成 - 淘宝、京东、拼多多...
✅ 数据同步 - 订单、商品、物流同步
```

### 9.1.2 支持的电商平台

**工程化思维**：配置化管理，支持动态添加

```python
# 内置默认平台（仅用于初始化）
BUILTIN_PLATFORMS = [
    {
        "platform_id": "taobao",
        "name": "淘宝/天猫",
        "icon": "🛒",
        "api_type": "taobao_api",
        "config_schema": {
            "app_key": {"type": "string", "label": "App Key"},
            "app_secret": {"type": "string", "label": "App Secret"}
        }
    },
    {
        "platform_id": "jd",
        "name": "京东",
        "icon": "📦",
        "api_type": "jd_api",
        "config_schema": {
            "app_key": {"type": "string", "label": "App Key"},
            "app_secret": {"type": "string", "label": "App Secret"}
        }
    },
    {
        "platform_id": "pdd",
        "name": "拼多多",
        "icon": "💰",
        "api_type": "pdd_api",
        "config_schema": {
            "client_id": {"type": "string", "label": "Client ID"},
            "client_secret": {"type": "string", "label": "Client Secret"}
        }
    },
    {
        "platform_id": "douyin",
        "name": "抖音商城",
        "icon": "🎵",
        "api_type": "douyin_api",
        "config_schema": {
            "app_key": {"type": "string", "label": "App Key"},
            "app_secret": {"type": "string", "label": "App Secret"}
        }
    },
    {
        "platform_id": "xianyu",
        "name": "闲鱼",
        "icon": "♻️",
        "api_type": "xianyu_api",
        "config_schema": {
            "api_key": {"type": "string", "label": "API Key"}
        }
    },
    {
        "platform_id": "xiaohongshu",
        "name": "小红书",
        "icon": "📕",
        "api_type": "xiaohongshu_api",
        "config_schema": {
            "app_id": {"type": "string", "label": "App ID"},
            "app_secret": {"type": "string", "label": "App Secret"}
        }
    }
]
```

---

## 9.2 DataProvider 数据提供器

### 9.2.1 抽象基类设计

**工程化思维**：接口隔离，易于扩展

```python
class DataProvider(ABC):
    """数据提供器抽象基类"""
    
    @abstractmethod
    def get_order(self, order_id: str, **kwargs) -> Optional[Dict[str, Any]]:
        """获取订单数据"""
        pass
    
    @abstractmethod
    def get_product(self, product_id: str, **kwargs) -> Optional[Dict[str, Any]]:
        """获取商品数据"""
        pass
    
    @abstractmethod
    def search_products(self, keyword: str, **kwargs) -> list:
        """搜索商品"""
        pass
    
    @abstractmethod
    def search_knowledge_base(self, query: str, **kwargs) -> list:
        """搜索知识库"""
        pass
    
    @abstractmethod
    def get_faqs(self, question: str, **kwargs) -> Optional[Dict[str, Any]]:
        """获取FAQ"""
        pass
```

### 9.2.2 ConfigurableEcommerceDataProvider 实现（新增！）

**工程化思维**：支持企业配置的数据提供器

```python
class ConfigurableEcommerceDataProvider(DataProvider):
    """支持企业配置的电商平台数据提供器"""
    
    def __init__(self, enterprise_id: Optional[str] = None):
        self.enterprise_id = enterprise_id
        self.enabled_platforms = []
        self._load_config()
    
    def _load_config(self):
        """加载企业配置"""
        if self.enterprise_id:
            try:
                # 获取企业启用的平台
                platforms = EcommerceConfigService.get_enabled_platforms(self.enterprise_id)
                self.enabled_platforms = platforms
            except Exception:
                # 如果获取配置失败，使用默认配置
                self.enabled_platforms = list(DEFAULT_PLATFORMS.keys())
        else:
            # 没有企业ID，使用所有平台
            self.enabled_platforms = list(DEFAULT_PLATFORMS.keys())
    
    def _filter_by_platform(self, items: List[Dict[str, Any]]) -> List[Dict[str, Any]]:
        """根据企业配置过滤平台数据"""
        if not self.enterprise_id:
            return items
        return [item for item in items if item.get("platform") in self.enabled_platforms]
    
    def get_order(self, order_id: str, **kwargs) -> Optional[Dict[str, Any]]:
        """获取订单数据"""
        order = DEFAULT_ORDERS.get(order_id)
        if order and order.get("platform") in self.enabled_platforms:
            return order
        return None
    
    def get_product(self, product_id: str, **kwargs) -> Optional[Dict[str, Any]]:
        """获取商品数据"""
        product = DEFAULT_PRODUCTS.get(product_id)
        if product and product.get("platform") in self.enabled_platforms:
            return product
        return None
    
    def search_products(self, keyword: str, **kwargs) -> List[Dict[str, Any]]:
        """搜索商品"""
        results = []
        for p in DEFAULT_PRODUCTS.values():
            if p.get("platform") in self.enabled_platforms:
                if keyword.lower() in p["product_name"].lower():
                    results.append(p)
        return results
    
    def get_platforms(self) -> List[Dict[str, Any]]:
        """获取所有电商平台配置（考虑企业配置）"""
        return [
            {
                "id": pid, 
                "name": DEFAULT_PLATFORMS[pid]["name"], 
                "enabled": pid in self.enabled_platforms
            }
            for pid in DEFAULT_PLATFORMS.keys()
        ]
    
    def get_platform_orders(self, platform: str = None) -> List[Dict[str, Any]]:
        """获取指定平台的订单"""
        orders = list(DEFAULT_ORDERS.values())
        
        # 首先根据企业配置过滤
        orders = self._filter_by_platform(orders)
        
        # 然后根据平台参数过滤
        if platform:
            orders = [o for o in orders if o.get("platform") == platform]
        
        return orders
    
    def get_platform_products(self, platform: str = None) -> List[Dict[str, Any]]:
        """获取指定平台的商品"""
        products = list(DEFAULT_PRODUCTS.values())
        
        # 首先根据企业配置过滤
        products = self._filter_by_platform(products)
        
        # 然后根据平台参数过滤
        if platform:
            products = [p for p in products if p.get("platform") == platform]
        
        return products
```

---

## 9.3 电商工具详解

### 9.3.1 订单查询工具

```python
class EcommerceOrderTool(BaseTool):
    """电商订单查询工具"""
    
    name = "query_ecommerce_order"
    description = "查询电商平台订单"
    tool_type = "DATA_QUERY"
    
    parameters = [
        Parameter(
            name="platform_type",
            type="string",
            description="电商平台(taobao/jd/pdd/douyin/xianyu/xiaohongshu)",
            required=False
        ),
        Parameter(
            name="platform",
            type="string",
            description="电商平台(可选，同platform_type)",
            required=False
        ),
        Parameter(
            name="order_id",
            type="string",
            description="订单编号(可选)",
            required=False
        )
    ]
    
    def execute(self, **kwargs) -> ToolResult:
        """执行订单查询"""
        platform = kwargs.get("platform_type") or kwargs.get("platform")
        order_id = kwargs.get("order_id")
        
        # 检查是否有额外的平台查询方法
        if hasattr(self.data_provider, 'get_platform_orders'):
            if order_id:
                # 先尝试直接通过ID获取
                order = self.data_provider.get_order(order_id)
                if order:
                    return ToolResult(
                        success=True,
                        data=order
                    )
            
            # 获取平台订单列表
            orders = self.data_provider.get_platform_orders(platform)
            return ToolResult(
                success=True,
                data={
                    "platform": platform or "all",
                    "count": len(orders),
                    "orders": orders
                }
            )
        
        # 回退到基础查询
        if order_id:
            order = self.data_provider.get_order(order_id)
            if order:
                return ToolResult(success=True, data=order)
            return ToolResult(success=False, error=f"订单 {order_id} 未找到")
        
        return ToolResult(
            success=False,
            error="请提供订单编号或平台"
        )
```

### 9.3.2 商品查询工具

```python
class EcommerceProductTool(BaseTool):
    """电商商品查询工具"""
    
    name = "query_ecommerce_product"
    description = "查询电商平台商品"
    tool_type = "DATA_QUERY"
    
    parameters = [
        Parameter(
            name="product_id",
            type="string",
            description="商品ID(可选)",
            required=False
        ),
        Parameter(
            name="keyword",
            type="string",
            description="搜索关键词",
            required=False
        )
    ]
    
    def execute(self, **kwargs) -> ToolResult:
        product_id = kwargs.get("product_id")
        keyword = kwargs.get("keyword")
        
        if product_id:
            product = self.data_provider.get_product(product_id)
            if product:
                return ToolResult(success=True, data=product)
            return ToolResult(success=False, error=f"商品 {product_id} 未找到")
        
        if keyword:
            products = self.data_provider.search_products(keyword)
            return ToolResult(
                success=True,
                data={
                    "keyword": keyword,
                    "count": len(products),
                    "products": products
                }
            )
        
        return ToolResult(
            success=False,
            error="请提供商品ID或搜索关键词"
        )
```

### 9.3.3 平台管理工具

```python
class EcommercePlatformTool(BaseTool):
    """电商平台管理工具"""
    
    name = "manage_ecommerce_platform"
    description = "查询电商平台信息"
    tool_type = "DATA_QUERY"
    
    parameters = [
        Parameter(
            name="action",
            type="string",
            description="操作(list)",
            enum=["list"],
            required=True
        )
    ]
    
    def execute(self, **kwargs) -> ToolResult:
        action = kwargs.get("action")
        
        if action == "list":
            if hasattr(self.data_provider, 'get_platforms'):
                platforms = self.data_provider.get_platforms()
                return ToolResult(
                    success=True,
                    data={
                        "count": len(platforms),
                        "platforms": platforms
                    }
                )
        
        return ToolResult(
            success=False,
            error=f"不支持的操作: {action}"
        )
```

---

## 9.4 企业级配置化架构（新增！）

### 9.4.1 配置服务设计

**工程化思维**：完全解耦，支持动态配置

```python
class EcommerceConfigService:
    """电商配置管理服务 - 完全配置化版本"""

    @staticmethod
    def _initialize_registry():
        """初始化平台注册表（仅首次使用）"""
        db = next(get_db())
        existing = db.query(PlatformRegistry).count()
        if existing == 0:
            for platform in BUILTIN_PLATFORMS:
                registry = PlatformRegistry(
                    id=str(uuid4()),
                    platform_id=platform["platform_id"],
                    name=platform["name"],
                    icon=platform["icon"],
                    api_type=platform["api_type"],
                    config_schema=platform["config_schema"]
                )
                db.add(registry)
            db.commit()

    @staticmethod
    async def get_all_platforms() -> List[Dict[str, Any]]:
        """获取所有已注册的平台（支持动态添加）"""
        EcommerceConfigService._initialize_registry()
        db = next(get_db())
        registries = db.query(PlatformRegistry).all()
        return [
            {
                "platform_id": r.platform_id,
                "name": r.name,
                "icon": r.icon,
                "api_type": r.api_type,
                "config_schema": r.config_schema,
                "enabled": r.enabled
            }
            for r in registries
        ]

    @staticmethod
    async def add_platform(
        platform_id: str,
        name: str,
        icon: str = "🔧",
        api_type: str = "custom",
        config_schema: Dict[str, Any] = None
    ) -> Dict[str, Any]:
        """添加新平台（无需重启服务）"""
        EcommerceConfigService._initialize_registry()
        db = next(get_db())
        
        existing = db.query(PlatformRegistry).filter(
            PlatformRegistry.platform_id == platform_id
        ).first()
        if existing:
            raise ValueError(f"平台 {platform_id} 已存在")
        
        registry = PlatformRegistry(
            id=str(uuid4()),
            platform_id=platform_id,
            name=name,
            icon=icon,
            api_type=api_type,
            config_schema=config_schema or {}
        )
        db.add(registry)
        db.commit()
        
        return {
            "platform_id": registry.platform_id,
            "name": registry.name,
            "icon": registry.icon
        }

    @staticmethod
    async def get_enterprise_platforms(enterprise_id: str) -> List[Dict[str, Any]]:
        """获取企业的电商平台配置（从注册表读取）"""
        EcommerceConfigService._initialize_registry()
        all_platforms = await EcommerceConfigService.get_all_platforms()
        
        db = next(get_db())
        configs = db.query(EnterpriseEcommerceConfig).filter(
            EnterpriseEcommerceConfig.enterprise_id == enterprise_id
        ).all()
        
        config_dict = {c.platform_id: c for c in configs}
        
        result = []
        for platform in all_platforms:
            config = config_dict.get(platform["platform_id"])
            result.append({
                "platform_id": platform["platform_id"],
                "name": platform["name"],
                "icon": platform["icon"],
                "api_type": platform["api_type"],
                "config_schema": platform["config_schema"],
                "enabled": config.enabled if config else platform["enabled"],
                "has_config": config is not None,
                "api_config": config.api_config if config else {},
                "sync_config": config.sync_config if config else {}
            })
        
        return result
```

### 9.4.2 数据库模型设计

```python
class PlatformRegistry(Base):
    """平台注册表 - 支持动态添加新平台"""
    __tablename__ = "platform_registry"
    
    id = Column(String, primary_key=True, index=True)
    platform_id = Column(String, unique=True, index=True, nullable=False)
    name = Column(String, nullable=False)
    icon = Column(String, default="🔧")
    api_type = Column(String)
    config_schema = Column(JSON)
    enabled = Column(Boolean, default=True)
    created_at = Column(DateTime, default=datetime.utcnow)
    updated_at = Column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)

class EnterpriseEcommerceConfig(Base):
    """企业电商配置"""
    __tablename__ = "enterprise_ecommerce_config"

    id = Column(String, primary_key=True, index=True)
    enterprise_id = Column(String, index=True, nullable=False)
    platform_id = Column(String, index=True, nullable=False)
    platform_name = Column(String)
    enabled = Column(Boolean, default=True)
    api_config = Column(JSON)
    sync_config = Column(JSON)
    created_at = Column(DateTime, default=datetime.utcnow)
    updated_at = Column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)

class ToolConfig(Base):
    """工具配置"""
    __tablename__ = "tool_config"

    id = Column(String, primary_key=True, index=True)
    enterprise_id = Column(String, index=True, nullable=False)
    tool_name = Column(String, index=True, nullable=False)
    enabled = Column(Boolean, default=True)
    config = Column(JSON)
    created_at = Column(DateTime, default=datetime.utcnow)
    updated_at = Column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)
```

### 9.4.3 API 端点设计

```python
# 获取平台注册表
@app.get("/api/v1/ecommerce/registry/platforms")
async def get_platform_registry():
    """获取平台注册表 - 查看所有已注册平台"""
    try:
        platforms = await EcommerceConfigService.get_all_platforms()
        return platforms
    except Exception as e:
        raise HTTPException(status_code=500, detail=f"获取平台注册表失败: {str(e)}")

# 添加新平台
@app.post("/api/v1/ecommerce/registry/platforms")
async def add_platform(
    platform_id: str,
    name: str,
    icon: str = "🔧",
    api_type: str = "custom",
    config_schema: Optional[dict] = None
):
    """添加新平台 - 无需重启服务"""
    try:
        result = await EcommerceConfigService.add_platform(
            platform_id=platform_id,
            name=name,
            icon=icon,
            api_type=api_type,
            config_schema=config_schema
        )
        return result
    except ValueError as e:
        raise HTTPException(status_code=400, detail=str(e))
    except Exception as e:
        raise HTTPException(status_code=500, detail=f"添加平台失败: {str(e)}")
```

---

### 9.5 示例数据结构

#### 9.5.1 订单数据

**工程化思维**：支持企业管理员灵活配置

```python
class EcommerceConfigService:
    """电商配置管理服务"""

    @staticmethod
    async def get_enterprise_platforms(enterprise_id: str) -> List[Dict[str, Any]]:
        """获取企业的电商平台配置"""
        db = next(get_db())
        
        # 获取企业已配置的平台
        configs = db.query(EnterpriseEcommerceConfig).filter(
            EnterpriseEcommerceConfig.enterprise_id == enterprise_id
        ).all()
        
        config_dict = {c.platform_id: c for c in configs}
        
        # 合并默认平台和企业配置
        result = []
        for platform_id, platform_info in SUPPORTED_PLATFORMS.items():
            config = config_dict.get(platform_id)
            result.append({
                "platform_id": platform_id,
                "name": platform_info["name"],
                "icon": platform_info["icon"],
                "enabled": config.enabled if config else True,
                "has_config": config is not None,
                "api_config": config.api_config if config else {},
                "sync_config": config.sync_config if config else {}
            })
        
        return result

    @staticmethod
    async def update_platform_config(
        enterprise_id: str,
        platform_id: str,
        enabled: bool,
        api_config: Optional[Dict] = None,
        sync_config: Optional[Dict] = None
    ) -> Dict[str, Any]:
        """更新平台配置"""
        db = next(get_db())
        
        # 验证平台ID
        if platform_id not in SUPPORTED_PLATFORMS:
            raise ValueError(f"不支持的平台: {platform_id}")
        
        # 查找现有配置
        config = db.query(EnterpriseEcommerceConfig).filter(
            EnterpriseEcommerceConfig.enterprise_id == enterprise_id,
            EnterpriseEcommerceConfig.platform_id == platform_id
        ).first()
        
        if config:
            # 更新现有配置
            config.enabled = enabled
            if api_config:
                config.api_config = api_config
            if sync_config:
                config.sync_config = sync_config
            config.updated_at = datetime.utcnow()
        else:
            # 创建新配置
            config = EnterpriseEcommerceConfig(
                id=str(uuid.uuid4()),
                enterprise_id=enterprise_id,
                platform_id=platform_id,
                platform_name=SUPPORTED_PLATFORMS[platform_id]["name"],
                enabled=enabled,
                api_config=api_config or {},
                sync_config=sync_config or {}
            )
            db.add(config)
        
        db.commit()
        db.refresh(config)
        
        return {
            "platform_id": config.platform_id,
            "enabled": config.enabled,
            "api_config": config.api_config,
            "sync_config": config.sync_config,
            "updated_at": config.updated_at.isoformat()
        }

    @staticmethod
    async def get_tool_configs(enterprise_id: str) -> List[Dict[str, Any]]:
        """获取企业的工具配置"""
        db = next(get_db())
        
        # 获取企业已配置的工具
        configs = db.query(ToolConfig).filter(
            ToolConfig.enterprise_id == enterprise_id
        ).all()
        
        config_dict = {c.tool_name: c for c in configs}
        
        # 合并默认工具和企业配置
        result = []
        for tool_name, tool_info in SUPPORTED_TOOLS.items():
            config = config_dict.get(tool_name)
            result.append({
                "tool_name": tool_name,
                "name": tool_info["name"],
                "description": tool_info["description"],
                "category": tool_info["category"],
                "enabled": config.enabled if config else True,
                "config": config.config if config else {}
            })
        
        return result

    @staticmethod
    async def update_tool_config(
        enterprise_id: str,
        tool_name: str,
        enabled: bool,
        config: Optional[Dict] = None
    ) -> Dict[str, Any]:
        """更新工具配置"""
        db = next(get_db())
        
        # 验证工具名称
        if tool_name not in SUPPORTED_TOOLS:
            raise ValueError(f"不支持的工具: {tool_name}")
        
        # 查找现有配置
        tool_config = db.query(ToolConfig).filter(
            ToolConfig.enterprise_id == enterprise_id,
            ToolConfig.tool_name == tool_name
        ).first()
        
        if tool_config:
            # 更新现有配置
            tool_config.enabled = enabled
            if config:
                tool_config.config = config
            tool_config.updated_at = datetime.utcnow()
        else:
            # 创建新配置
            tool_config = ToolConfig(
                id=str(uuid.uuid4()),
                enterprise_id=enterprise_id,
                tool_name=tool_name,
                enabled=enabled,
                config=config or {}
            )
            db.add(tool_config)
        
        db.commit()
        db.refresh(tool_config)
        
        return {
            "tool_name": tool_config.tool_name,
            "enabled": tool_config.enabled,
            "config": tool_config.config,
            "updated_at": tool_config.updated_at.isoformat()
        }

    @staticmethod
    async def get_enabled_platforms(enterprise_id: str) -> List[str]:
        """获取企业启用的平台列表"""
        platforms = await EcommerceConfigService.get_enterprise_platforms(enterprise_id)
        return [p["platform_id"] for p in platforms if p["enabled"]]

    @staticmethod
    async def get_enabled_tools(enterprise_id: str) -> List[str]:
        """获取企业启用的工具列表"""
        tools = await EcommerceConfigService.get_tool_configs(enterprise_id)
        return [t["tool_name"] for t in tools if t["enabled"]]
```

#### 9.4.2 数据模型

```python
class EnterpriseEcommerceConfig(Base):
    """企业电商配置"""
    __tablename__ = "enterprise_ecommerce_config"

    id = Column(String, primary_key=True, index=True)
    enterprise_id = Column(String, index=True, nullable=False)
    platform_id = Column(String, index=True, nullable=False)
    platform_name = Column(String)
    enabled = Column(Boolean, default=True)
    api_config = Column(JSON)  # API密钥等配置
    sync_config = Column(JSON)  # 同步配置
    created_at = Column(DateTime, default=datetime.utcnow)
    updated_at = Column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)

class ToolConfig(Base):
    """工具配置"""
    __tablename__ = "tool_config"

    id = Column(String, primary_key=True, index=True)
    enterprise_id = Column(String, index=True, nullable=False)
    tool_name = Column(String, index=True, nullable=False)
    enabled = Column(Boolean, default=True)
    config = Column(JSON)  # 工具特定配置
    created_at = Column(DateTime, default=datetime.utcnow)
    updated_at = Column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)
```

#### 9.4.3 API端点

```python
# 在 chat-service/main.py 中添加
@app.get("/api/v1/ecommerce/platforms")
async def get_ecommerce_platforms(enterprise_id: UUID):
    """获取企业电商平台配置"""
    try:
        platforms = await EcommerceConfigService.get_enterprise_platforms(str(enterprise_id))
        return platforms
    except Exception as e:
        raise HTTPException(status_code=500, detail=f"获取平台配置失败: {str(e)}")

@app.put("/api/v1/ecommerce/platforms/{platform_id}")
async def update_ecommerce_platform(
    enterprise_id: UUID,
    platform_id: str,
    enabled: bool,
    api_config: Optional[dict] = None,
    sync_config: Optional[dict] = None
):
    """更新企业电商平台配置"""
    try:
        result = await EcommerceConfigService.update_platform_config(
            enterprise_id=str(enterprise_id),
            platform_id=platform_id,
            enabled=enabled,
            api_config=api_config,
            sync_config=sync_config
        )
        return result
    except ValueError as e:
        raise HTTPException(status_code=400, detail=str(e))
    except Exception as e:
        raise HTTPException(status_code=500, detail=f"更新平台配置失败: {str(e)}")

@app.get("/api/v1/ecommerce/tools")
async def get_ecommerce_tools(enterprise_id: UUID):
    """获取企业电商工具配置"""
    try:
        tools = await EcommerceConfigService.get_tool_configs(str(enterprise_id))
        return tools
    except Exception as e:
        raise HTTPException(status_code=500, detail=f"获取工具配置失败: {str(e)}")

@app.put("/api/v1/ecommerce/tools/{tool_name}")
async def update_ecommerce_tool(
    enterprise_id: UUID,
    tool_name: str,
    enabled: bool,
    config: Optional[dict] = None
):
    """更新企业电商工具配置"""
    try:
        result = await EcommerceConfigService.update_tool_config(
            enterprise_id=str(enterprise_id),
            tool_name=tool_name,
            enabled=enabled,
            config=config
        )
        return result
    except ValueError as e:
        raise HTTPException(status_code=400, detail=str(e))
    except Exception as e:
        raise HTTPException(status_code=500, detail=f"更新工具配置失败: {str(e)}")
```

---

### 9.5 示例数据结构

#### 9.5.1 订单数据

```python
# 默认电商订单示例
DEFAULT_ORDERS = {
    "taobao_1001": {
        "order_id": "taobao_1001",
        "platform": "taobao",
        "platform_order_id": "3216549870123",
        "product_name": "女士连衣裙",
        "customer_name": "王女士",
        "customer_phone": "136****8888",
        "order_amount": 358,
        "order_status": "已签收",
        "created_at": "2024-01-10T09:15:00"
    },
    "jd_2002": {
        "order_id": "jd_2002",
        "platform": "jd",
        "platform_order_id": "JD1234567890",
        "product_name": "男士运动鞋",
        "customer_name": "赵先生",
        "customer_phone": "137****9999",
        "order_amount": 599,
        "order_status": "配送中",
        "created_at": "2024-01-14T16:45:00"
    }
}
```

### 9.4.2 商品数据

```python
# 默认电商商品示例
DEFAULT_PRODUCTS = {
    "taobao_prod001": {
        "product_id": "taobao_prod001",
        "platform": "taobao",
        "product_name": "夏季雪纺连衣裙",
        "price": 258,
        "stock": 30,
        "sales_count": 1250,
        "platform_url": "https://item.taobao.com/item.htm?id=123456"
    },
    "jd_prod001": {
        "product_id": "jd_prod001",
        "platform": "jd",
        "product_name": "Nike Air Max运动鞋",
        "price": 899,
        "stock": 15,
        "sales_count": 680,
        "platform_url": "https://item.jd.com/12345678.html"
    }
}
```

---

## 9.6 工具集成流程

### 9.6.1 初始化与加载（支持企业配置）

```python
# 1. 创建支持企业配置的数据提供器
enterprise_id = "ent_12345"
data_provider = ConfigurableEcommerceDataProvider(enterprise_id=enterprise_id)

# 2. 加载并注册工具
from chat_service.tools.loader import load_tools
tool_count = load_tools(data_provider)
print(f"已加载 {tool_count} 个工具")
```

### 9.6.2 完整对话流程

```
用户："查一下淘宝订单"
  ↓
Agent 意图识别 → 需要调用订单查询工具
  ↓
Agent 参数提取 → platform_type="taobao"
  ↓
Tool Executor 执行
  ↓
EcommerceOrderTool.execute(platform_type="taobao")
  ↓
DataProvider.get_platform_orders("taobao")
  ↓
返回订单数据
  ↓
Agent 整理结果 → "您在淘宝有2个订单..."
  ↓
返回用户
```

---

## 9.7 工程化最佳实践

### 9.7.1 配置化架构（新增！）

**工程化思维**：支持企业管理员灵活配置

```python
# 1. 数据库模型设计
# - 使用 JSON 字段存储灵活的配置
# - 支持企业级隔离
# - 记录配置变更历史

# 2. 配置加载策略
# - 优先加载企业配置
# - 降级使用默认配置
# - 配置变更自动生效

# 3. 数据过滤机制
# - 配置化的数据访问控制
# - 动态启用/禁用平台
# - 工具级别的权限控制
```

### 9.7.2 多平台适配

**工程化设计**：策略模式

```python
# 不同平台可以有不同的实现
class TaobaoDataProvider(DataProvider):
    """淘宝数据提供器"""
    def get_order(self, order_id: str):
        # 调用淘宝API
        pass

class JdDataProvider(DataProvider):
    """京东数据提供器"""
    def get_order(self, order_id: str):
        # 调用京东API
        pass

# 工厂方法
def create_data_provider(platform: str) -> DataProvider:
    providers = {
        "taobao": TaobaoDataProvider,
        "jd": JdDataProvider,
        "pdd": PddDataProvider,
    }
    return providers.get(platform, MockDataProvider)()
```

### 9.7.3 错误处理与降级

**产品化思维**：用户体验优先

```python
def execute(self, **kwargs) -> ToolResult:
    try:
        # 尝试调用真实API
        order = self.data_provider.get_order_from_api(order_id)
        return ToolResult(success=True, data=order)
    
    except APITimeoutError:
        # API超时，尝试查询缓存
        cached_order = self.data_provider.get_from_cache(order_id)
        if cached_order:
            return ToolResult(
                success=True,
                data=cached_order,
                metadata={"source": "cache", "note": "数据可能有延迟"}
            )
        return ToolResult(
            success=False,
            error="订单查询服务暂时不可用，请稍后再试"
        )
    
    except Exception as e:
        logger.error(f"订单查询失败: {e}")
        return ToolResult(
            success=False,
            error="查询订单时出现问题，请稍后再试"
        )
```

### 9.7.4 缓存策略

**工程化**：减少API调用，提升响应速度

```python
from functools import lru_cache
import time

class CachedEcommerceDataProvider(ConfigurableEcommerceDataProvider):
    """带缓存的电商数据提供器"""
    
    def __init__(self, enterprise_id: Optional[str] = None):
        super().__init__(enterprise_id)
        self._order_cache = {}
        self._product_cache = {}
        self._cache_ttl = 300  # 5分钟缓存
    
    def get_order(self, order_id: str):
        # 检查缓存
        if order_id in self._order_cache:
            cached_data, cached_time = self._order_cache[order_id]
            if time.time() - cached_time < self._cache_ttl:
                return cached_data
        
        # 查询真实数据
        order = super().get_order(order_id)
        
        # 更新缓存
        if order:
            self._order_cache[order_id] = (order, time.time())
        
        return order
```

---

## 9.8 产品化优化

### 9.8.1 用户友好的响应格式

```python
# 原始数据
{
    "order_id": "taobao_1001",
    "platform": "taobao",
    "product_name": "女士连衣裙",
    "order_amount": 358,
    "order_status": "已签收"
}

# 产品化后的友好格式
{
    "order_id": "taobao_1001",
    "platform": "淘宝",
    "platform_name": "淘宝",
    "product_name": "女士连衣裙",
    "order_amount": "¥358.00",
    "order_status": "已签收",
    "status_text": "订单已签收",
    "order_date": "2024-01-10",
    "customer_display": "王女士 (136****8888)"
}
```

### 9.8.2 敏感数据脱敏

**产品化**：隐私保护

```python
def mask_sensitive_data(order: Dict) -> Dict:
    """脱敏处理"""
    masked = order.copy()
    
    # 手机号脱敏
    if "customer_phone" in masked:
        phone = masked["customer_phone"]
        if len(phone) >= 11:
            masked["customer_phone"] = phone[:3] + "****" + phone[7:]
    
    # 姓名脱敏
    if "customer_name" in masked:
        name = masked["customer_name"]
        if len(name) >= 2:
            masked["customer_name"] = name[0] + "*" * (len(name) - 1)
    
    return masked
```

---

## 9.9 本章小结

### 核心概念

| 组件 | 作用 |
|------|------|
| `ConfigurableEcommerceDataProvider` | 支持企业配置的电商数据提供器 |
| `EcommerceOrderTool` | 订单查询工具 |
| `EcommerceProductTool` | 商品查询工具 |
| `EcommercePlatformTool` | 平台管理工具 |
| `EcommerceConfigService` | 电商配置管理服务 |

### 工程化思维要点

1. **抽象接口** - DataProvider统一接口
2. **配置化架构** - 支持企业管理员灵活配置
3. **策略模式** - 多平台适配
4. **缓存优化** - 减少API调用
5. **错误处理** - 优雅降级
6. **数据隔离** - 企业级数据安全

### 产品化思维要点

1. **用户体验** - 友好的响应格式
2. **隐私保护** - 敏感数据脱敏
3. **多平台支持** - 淘宝/京东/拼多多...
4. **容错机制** - API超时/失败处理
5. **数据展示** - 金额格式化/状态翻译
6. **灵活配置** - 企业管理员自主管理

### 学习回顾

恭喜你完成了工具系统的学习！现在你已经掌握了：

✅ 工具注册与执行机制
✅ 电商工具的实现原理
✅ 企业级配置化架构
✅ 工程化与产品化思维
✅ 多平台数据集成方案

接下来可以学习其他章节，或动手实践添加新工具！参考 [43_开发实践2-配置电商工具](25_开发实践2-配置电商工具.md)。
